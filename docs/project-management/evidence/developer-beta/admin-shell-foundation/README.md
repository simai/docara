# Developer Beta Docara shell consumer

Status: review-ready

This packet records the bounded `larena/docara` presentation slice that renders
the Page list and form inside the shared `larena/admin` shell.
