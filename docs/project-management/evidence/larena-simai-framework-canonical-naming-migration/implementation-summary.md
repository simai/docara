# Implementation summary

- Smart field manifests now request `ui.sf_element`.
- Active package copy uses `Simai Framework` without a version-family label.
- Test fixture and method names use the canonical `Sf`/`sf` vocabulary.
- No document persistence or publication behavior changed.
