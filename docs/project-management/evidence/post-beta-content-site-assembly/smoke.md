# Smoke

`https://larena.test/admin/docara/pages` rendered the persisted `test` page in Russian. At 390×844 the document width remained 390 px and table rows became labelled cards. `https://larena.test/docs/test` rendered one semantic main/article and loaded `/larena/assets/docara/docara.public.page.css` with owner `larena/core:core.assets`.
