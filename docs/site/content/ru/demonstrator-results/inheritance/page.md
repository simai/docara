# Наследование настроек

Раздел задаёт компактную ширину и отключает оглавление с соседними переходами.
Страница наследует эти значения, но локально возвращает широкую область
контента.

## Откуда пришло значение

В resolved page plan можно увидеть итоговую конфигурацию и provenance каждого
поля.
