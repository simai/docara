# Footer из безопасных блоков

Footer выключен по умолчанию. Эта страница включает область и наполняет её
зарегистрированной секцией с двумя структурированными HTML-элементами.

## Почему не строка HTML

JSON задаёт семантический тег, текст, локальную ссылку и утилиты Framework.
Скрипты, обработчики событий и произвольные шаблоны недоступны автору.
