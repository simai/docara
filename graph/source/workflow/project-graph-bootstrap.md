# Bootstrap evidence: Docara unified graph

- Created: 2026-08-01
- Repository: `/Users/rim/Documents/GitHub/docara-unified`
- Branch: `codex/docara-unified-architecture`
- Exact base: `a3ba9a4d04429f1f2046b8415764fe7bc89962c7`
- Human specification: `docs/specification/README.md`
- Primary goal: `docara.goal.unified`
- Ready batch: `docara.batch.m0.mapping`

The graph was initialized through the Federation graph tooling and then
replaced with project-specific objects. Template `example-*` objects are not
part of the canonical graph.

The installed Docara skill is intentionally excluded because it describes the
retired architecture. This is a recorded routing gap, not permission to use
chat history as an alternative source of truth. The bounded fallback is
`teamlead + graph + dev`, with `content`, `ux` and `tester` as companions when
their contracts are exercised.

No runtime, release, default branch, tag or deployment was modified by this
bootstrap.
