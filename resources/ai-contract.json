{
    "schema": "docara.ai_contract.v1",
    "version": "1.1.0",
    "product_major": 2,
    "skill_contract": ">=1.0.0 <2.0.0",
    "compatibility": "One canonical skill must inspect the exact project-local capabilities contract before selecting operations.",
    "fallback": "When capabilities is unavailable, use the documented legacy compatibility path and never assume newer commands."
}
