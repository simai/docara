<!-- docara-variant:base -->
:::card
### Portable project

Markdown and JSON stay next to the code and can be reviewed through Git.
:::

<!-- docara-variant:parameter.variant.default -->
:::card {variant=default}
### Decorated card

The surface, border and inner spacing are applied automatically.
:::

<!-- docara-variant:parameter.variant.plain -->
:::card {variant=plain}
### Without a border or inner spacing

Use this variant inside `grid` when the card does not need its own surface.
:::
