# Track Decisions

- Extend `FrameworkAssetPlanner`; do not create another asset planner or cache.
- Treat compatible package metadata as the authoritative preload contract.
- Preload only complete CSS/JavaScript dependency closures.
- Keep non-critical content components on the dynamic Framework loader.
- Preserve natural typography-dependent header height.
