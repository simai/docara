# Docara Goal C-C1 — catalog, settings and agent-journey truthfulness correction

Date: 2026-08-05
Status: `goal_c_independently_accepted`
Parent goal: Goal C — Public Documentation, Settings Reference & Agent Journey
Rejected product candidate: `ae6a1e918e248517b728cf40460d6c359991b66e`
Rejected governance HEAD: `26a212aff337254f75cf4df426c0684e5ee8f4e9`
Audit marker: `DOCARA-AUTO-AUDIT:019fd18a-2f52-7133-a7bb-bcc8eb9f5d14`

## Goal

Make the six public component groups, schema-derived settings reference and
CLI/JSON/MCP journey describe and execute the admitted product contract, then
rebuild all Goal C evidence on one new exact candidate.

## Done When

- the six pages are populated from one deterministic admitted projection with
  independent namespace, owner package, provider, origin, authoring kind and
  support facets;
- inline contains `docara.badge`, `docara.button`, `docara.icon`, `docara.kbd`;
  Framework contains the exact supported `ui.*` artifacts; typed blocks and
  content containers are distinct, with `docara.grid` and machine contracts;
- selected schema surfaces traverse internal refs, object/array branches,
  `oneOf`/`anyOf`/`allOf` and conditionals, and every row has an exact JSON
  pointer plus meaningful validation;
- documented CLI/JSON/MCP commands execute in a disposable project with
  read-only, root, dry-run and exact hash-bound apply boundaries intact;
- focused/full, deterministic build/full-single, static, package/consumer,
  security, browser/accessibility/SEO and graph/context checks are fresh;
- current graph, specification, workflow, handoff and evidence name one new
  `goal_c_ready_for_independent_audit` candidate; no self-acceptance.

## Constraints

- one Markdown prose owner per page and shared UI copy only in `lang.json`;
- one typed IR, renderer registry, Smart Gateway, LayoutComposer and PageBuilder;
- no component-ID dispatch/list, unsupported Framework claim, arbitrary
  executable/template path, external repository/site write or Goal D action;
- rollback by reverting correction commits to governance HEAD `26a212a…`.

## Batches

| Batch | Work | Verification | Status |
| --- | --- | --- | --- |
| C1.0 | Freeze baseline and preserve RED reproductions | exact audit facts and focused RED commands | complete |
| C1.1 | Admitted catalog facets and six rendered outcomes | real docs/site runtime tests | complete |
| C1.2 | Exhaustive schema projection | pointer/branch/constraint tests | complete |
| C1.3 | Executable CLI/JSON/MCP journey | disposable-project command test | complete |
| C1.4 | Integrated exact-candidate retest | full matrix and fresh hashes | complete |
| C1.5 | Governance/handoff sync | graph/context/diff/clean checks | complete |

## Evidence

`source/workflow/evidence/2026-08-05-docara-goal-c-c1-truthfulness-correction/INDEX.md`

## Independent result and next boundary

Independent reverse-outcome audit
`DOCARA-AUTO-AUDIT:a270c9dd-9c4b-418b-8b5b-6e00f9b65397` accepted exact
product candidate `eb35f5c6f18e5eb9be69e91887b09486f5703136` with `PASS`.
The product track is complete. Goal D was not created; release review, merge,
tag and deploy remain unauthorized until an explicit user decision.
