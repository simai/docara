# Batch 9 — exact Human-Centered Simplicity/source/docs/security verdict

Date: 2026-07-19
Goal baseline: `31f468be85d015b962fccc2b4c089204aab1410b`
Candidate: `de87bdef224d518d1c707286d4640be0238d34bc`
Tree: `7c0c20678aff65858e29e9be4dd304ddd44ba17b`
Verdict: `PASS`

## Complete-diff review

All 243 paths in the exact baseline-to-candidate diff were inventoried and
classified; unexplained paths: zero.

- 159 additions, 77 modifications and seven deletions;
- complete name-status fingerprint:
  `f34b10f5d4ee18d4308655088a59355392340a59155091951a4d95d10bfbcc17`;
- full-index binary diff fingerprint:
  `baa460d73a3d37c0eab12f51021fc9815fcc5b42995d4d02e92db770e34d4a41`.

The complete vertical remains one portable product model for reader, author,
migrator and maintainer. Generated catalogue pages consume one effective
projection rather than a second registry; Markdown remains the content
language; JSON owns configuration and typed facts; Framework locks and
deterministic receipts remain protective complexity rather than new
author-facing concepts.

## Bounded correction review

The direct parent-to-candidate product/test correction is limited to:

1. one existing mobile CSS reserve changing from `8rem` to `10rem`;
2. one source/output regression assertion;
3. a test-only incremental drain of existing Symfony Process stdout/stderr
   pipes before raw-socket requests.

The production static router and `ServeCommand` blobs are byte-identical to
the parent. No selector, JavaScript scroll interception, retry, dependency,
schema, asset, registry or Framework-owner surface was added. The remaining
six changed paths are honest workflow and evidence records.

## Source, documentation and security

- task-oriented documentation covers beginner, author, migration, maintainer
  and extension-developer/AI paths;
- product naming remains `Simai Framework`; no version shorthand appears in
  product, documentation or test surfaces;
- consumed Core, Smart and registry references are immutable commits with
  exact hashes;
- no consumed moving reference was found;
- raw HTML is stripped, dynamic output is escaped, search remains same-origin
  and router containment remains fail-closed;
- high-signal secret, unsafe DOM sink, product-private path and readiness
  overclaim scans found no blocker;
- private operational paths remain confined to export-excluded
  `source/workflow`.

## Independent verification

- full no-TTY PHPUnit: 541 tests, 4,305 assertions, PASS;
- mobile anchor focus: one test, 38 assertions, PASS;
- static-router focus: five of five runs, 20 assertions each, PASS;
- two clean builds and static verification: PASS;
- both builds have digest
  `502e43119ea2f2fc6ce358042858937060a67c4aa3d4d5ac0295e3d19c8e782f`;
- source formatting, lint, structured parsing, diff and repository hygiene:
  PASS.

An initial PTY run waited on terminal-input transport and produced no test
verdict. It had no child server and was terminated only after the equivalent
no-TTY run passed. This was an acceptance-transport anomaly, not a product
failure.

The complete independent report has SHA-256
`b205fca61d69fd07e6c59ae59ef1601b6673b904b7eb56767d4218c2fedada54`.
This slice does not claim browser, publication, production or public-release
acceptance.
