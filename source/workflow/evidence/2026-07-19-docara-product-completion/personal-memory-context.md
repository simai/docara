# Personal memory context

Date: 2026-07-19
Decision: `inject`

## Used context

Prior Docara recovery established a regression contour for recursive left-menu
behavior: preserve direct navigation, test submenu disclosure with real browser
interaction, prevent horizontal overflow, and keep local build publication
separate from source-repository release claims.

## Verification policy

Memory-derived context is orientation only. Before implementation, verify current Git
revision and worktree, renderer and styles, pinned Framework manifests and
examples, current `docara.test` HTML/browser behavior, local build tooling,
backup/rollback state and every external reference used for a design decision.
Do not reuse old paths, versions, screenshots or runtime results as current
evidence without that verification.
