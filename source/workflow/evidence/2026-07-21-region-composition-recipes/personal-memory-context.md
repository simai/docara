# Personal memory context

Prior context was used only to preserve Docara's established portable-build,
primary-generator and local ServBay publication boundaries. It did not replace
current repository, test, generated-output or browser evidence.

## Verification policy

Memory-derived context never proves current state. Required verification was
performed on the current worktree and served local site: exact baseline and
legacy digest, schemas and runtime behavior, full test suite, duplicate clean
builds, static references, served manifest, responsive layouts, themes and
console state. Current evidence always overrides memory.
