# M3.1 durable execution contract

Date: 2026-08-01

Verdict: PASS

Parent SHA: `b14fe4e1e70a5465fe382bd5ced1de26cb65a315`

Candidate binding: the commit that first contains this evidence file. Resolve
with `git log --format='%H' --follow --
source/workflow/evidence/2026-08-01-docara-unified-architecture/m3-ru-components/M3.1-EXECUTION-CONTRACT.md`.

## Outcome

M3 is one live goal for the complete Russian component section, not a series
of independently completed goal claims. The recovery contract defines 30
bounded implementation/acceptance batches, six milestones and one Completion
Gate. M3.1 changes documentation, graph and evidence only.

The accepted Alert plan remains the first vertical slice. Its independent
PASS authorizes implementation inside M3.2 but does not establish migration or
product readiness.

## Exact inventory

- public routes under `/ru/components/`: 32;
- physical Markdown owners: 2 (`badge.md`, `syntax.md`);
- generated routes: 30, consisting of 29 detail projections and the component
  index;
- target: 32 physical owners under
  `docs/site/content/ru/components/<slug>.md`;
- Russian language-pack component entries: 42 at the parent revision;
- route/source/definition/example/target/family mapping:
  `route-owner-inventory.json`.

## Reproducible baseline

The baseline was produced from a clean archive of the parent revision using
the repository CLI and the accepted dependency tuple.

- full build A: 103 pages, 321 files;
- full build B: 103 pages, 321 files;
- A/B output trees: byte-identical;
- manifest SHA-256:
  `7c9aa1ed9d3617fe98cf8e463c92c5fed72481910597da26e89cd9c40a33c105`;
- static verification: 206 HTML documents, 18,866 local references, zero
  broken references;
- every one of the 32 component routes: isolated tree equals the corresponding
  full-build tree and page HTML hash;
- browser baseline: index, Alert, Tabs, Table, Media and Tree at 1440 light,
  1920 dark and 390 light/dark; 24 captures; zero console errors/warnings.

Exact commands, route hashes, global artifact hashes and corrected diagnostic
attempts are stored in `baseline.json`. Capture hashes and coverage are stored
in `browser-captures.json`; screenshots are disposable evidence, never an
authoring source.

## Migration order

1. early isolated-route selection;
2. generic block-component IR/compiler contract;
3. single renderer registry and Smart gateway coverage;
4. Alert physical Markdown owner;
5. native/interactive/layout/media/code component families;
6. physical component index and derived views;
7. zero-reference legacy reduction;
8. deterministic/static/browser/reverse acceptance.

The detailed 30-batch sequence is authoritative in
`source/workflow/2026-08-01-docara-m3-ru-components-goal.md`.

## Control-plane evidence

- release-context, runtime-naming and source-policy preflight gates: PASS;
- installed obsolete Docara owner remained disabled and unused;
- route classification and inherited-launch parser mismatches are recorded as
  federation gaps; project-local `general_delivery` workflow/process check:
  PASS;
- official project graph validator: PASS, 1 goal, 6 stages, 8 batches,
  4 metrics, 6 implementation mappings, zero warnings and zero blockers;
- graph/evidence JSON, launch YAML and `git diff --check`: PASS;
- no runtime, content, resource, dependency or lock file changed in M3.1;
- rollback: revert the single M3.1 documentation/graph/evidence commit.

## Readiness boundary

M3.1 proves inventory and a stable starting point only. It does not claim an
implemented route selector, Alert migration, global Markdown ownership,
legacy retirement, M3 completion, release or production readiness. The next
bounded checkpoint is M3.2 runtime/Alert, starting with batch 03.
