# M3.2: shared runtime and Alert vertical slice

Date: 2026-08-01

Verdict: PASS

Parent SHA: `843d4cf38c17385854d3fa2a6dbafebd17cc169f`

Candidate binding: the commit that first contains this evidence file.

## Outcome

`/ru/components/alert/` is now owned by
`docs/site/content/ru/components/alert.md`. The page uses the same early route
selector, `PageBuilder`, typed in-memory Document IR, renderer registry and
Smart gateway as Badge. No Alert-specific engine, PageBuilder, registry or
gateway was added.

The physical page contains reader-facing purpose, one tabbed general example,
five semantic variants, parameter tables, a call example and the useful
color-independent accessibility limit. Its five `component_block` nodes use
the generic manifest/template contract; the example/compiler path preserves
the physical source span even when the block is inside fenced Markdown.

## Owner and legacy reduction

- old page prose owner: `resources/language-packs/ru.json#components.docara.alert`;
- old example projection source:
  `resources/component-catalog/examples/docara.alert.ru.md`;
- new owner: `docs/site/content/ru/components/alert.md`;
- portable starter owner:
  `stubs/portable/content/ru/components/alert.md`;
- generated route allowlist: `/ru/components/alert/` removed;
- Russian generated-route maximum: `42 -> 41`;
- Russian component presentation record: removed;
- generated Russian component-detail receipt: `29 -> 28`; Alert absent;
- retired example source pre-deletion SHA-256:
  `b1576ae7bd5e15935d40e02c744c990deb0fcea51aaacffc8594e0936ecd7601`.

The scoped active-code/content/resource scan found zero references to
`docara.alert.ru.md` before deletion. Historical workflow/handoff records are
retained as evidence. Rollback is the revert of this checkpoint commit; the
deleted bytes and previous pack/allowlist entries remain recoverable from its
parent.

## Runtime contract

```text
physical Markdown
  -> MarkdownCompiler (typed Document IR in memory)
  -> DocumentRendererRegistry[component_block]
  -> ComponentBlockDocumentNodeRenderer
  -> SmartComponentGateway::renderComponentBlock()
  -> ContentComponentRenderer
  -> structural manifest/template
  -> PageBuilderResult
```

`:::example` now treats fenced Markdown as a true boundary: an inner `:::` no
longer terminates the outer example. Typed block nodes within that fence are
validated with their physical line/column. The compatibility Markdown Alert
adapter delegates its final artifact to the same content Smart gateway; the
former duplicate manual Alert HTML assembly is gone.

## Build and static verification

Disposable runtime: `/tmp/docara-m3-runtime.WXaV0l`.

```text
php ../../docara build m3-alert-tabs2-full
PASS — 103 pages, 321 files

copy full tree, then:
php ../../docara build m3-alert-tabs2-single --page=/ru/components/alert/
PASS — 1 selected page

diff -qr build_m3-alert-tabs2-full build_m3-alert-tabs2-single
PASS — no differences

php ../../docara verify-static build_m3-alert-tabs2-full
PASS — 206 HTML pages, 18,872 local references, 0 broken
```

- full tree manifest SHA-256:
  `e22977096fc7cfe07d77b8e1bc68eb6d26cbc3ef63f491d5a8d6ed9664649884`;
- Alert full/single HTML SHA-256:
  `51897e5fc51ba73f2118e895065bf8d35c634c103206794d6e3e03d24a3c1e75`;
- diagnostics: `page_path=content/ru/components/alert.md`,
  `page_source_kind=authored_markdown`, output
  `ru/components/alert/index.html`;
- output contract: five Alert blocks, one example surface, Example/Markdown
  tabs, one example copy action, two responsive tables;
- declared Alert assets remain empty; site asset URLs and dependency/lock
  tuple are unchanged.

The accepted generated-page baseline hash
`a12740106a16dc42a4916c53e5f33f65fb4e18f527eaec857825f712f559dec9`
intentionally changes: its catalog-owned technical wrapper and prose are
replaced by the required human-authored Markdown article. URL, five semantic
variants, roles, responsive layout, tabs/code/copy capability and product
styling are preserved. Exact parity claimed here is full versus isolated on
the new physical owner, not byte identity to retired generated prose.

## Tests

```text
php vendor/bin/phpunit tests/Unit/MarkdownCompilerTest.php \
  tests/Unit/PortableMarkdownRendererTest.php tests/Unit/PageBuilderTest.php \
  tests/Unit/LegacyPublicSourceAllowlistTest.php \
  tests/Unit/PortableComponentCatalogProjectorTest.php \
  tests/Unit/EffectiveComponentCatalogTest.php
PASS — 91 tests, 3,774 assertions, 2 inherited warnings

php vendor/bin/phpunit
PASS — 368 tests, 7,347 assertions, 2 inherited warnings
```

Pint, changed-file PHP lint, JSON parsing and `git diff --check`: PASS.

## Browser matrix

Chromium/Playwright 1.52, settled after network idle:

| View | Theme | SHA-256 | Result |
| --- | --- | --- | --- |
| desktop 1440 | light | `e910f98079e94f3ef7d1498d24b38e1c1176104309abd2b29d6912e8b29d174d` | PASS |
| desktop 1440 | dark | `d3510aea62a6d30f80424c273be913a068c15e37d94f784ed5f2ebe0918731d6` | PASS |
| mobile 390 | light | `12b77be8b1000a616359c4abd0258333b1f6aae758355e878854e52259b3b760` | PASS |
| mobile 390 | dark | `9b72995ae76b30e9c64ff31421cef519612afb68b8671f5ed30d9cfda619bc93` | PASS |

Each mode proves: title, 5 Alerts, 4 `status` plus 1 `alert` roles, visible
Framework colors/border, 1 tabbed example, source/preview switching, copy
state, 2 responsive tables, no horizontal overflow, first Tab on the skip
link, and zero console/page errors or warnings. Screenshots are disposable
evidence under `/tmp/.../output/playwright/m3-alert-tabs/`, not a source.

## Reproducibility notes

- the unpinned latest Playwright CLI requested an unavailable browser
  revision; the installed cached Playwright 1.52/browser pair was used;
- PHP resolved to a stale Homebrew 8.2 binary inside one temporary-directory
  invocation and failed on missing ICU 73; the installed ServBay PHP 8.4.20
  runtime used by the successful suite was selected explicitly;
- an early browser assertion looked for nonexistent `data-copied`; the runtime
  contract uses `data-copy-state=copied`, and the corrected matrix passed;
- an early copy to the disposable tree used zsh's special `path` variable and
  left the Markdown stale; the final `tabs2` builds use an explicit `rel`
  variable and contain the verified tabbed owner.

## Nonclaims and next step

M3.2 is complete. Overall M3 remains in progress: 3 of 32 component routes
are physical and 29 remain generated, including the component index. Global
source ownership, derived-view convergence, complete legacy retirement,
release and production readiness are not claimed. Batch 07 starts M3.3 with
native headings/text and lists/quotes.
