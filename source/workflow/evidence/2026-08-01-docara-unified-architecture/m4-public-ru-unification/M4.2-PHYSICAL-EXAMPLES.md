# M4.2 physical examples migration

Status: PASS

Parent: `17d1152`

Candidate: commit containing this evidence

## Result

- all 14 `/ru/examples/` routes are selected as `authored_markdown`;
- owners are `docs/site/content/ru/examples.md` and thirteen files under
  `docs/site/content/ru/examples/`;
- the build still selects 103 pages, now 103 physical and zero generated;
- existing `/ru/demonstrator-results/.../` owners remain canonical live
  results; no demonstrator prose was copied into a second result page;
- titles, descriptions, categories, source labels and links are in Markdown;
  adjacent JSON contains only navigation order;
- the generic `internal_preview` typed directive uses the bundled typed
  definition repository and renderer registry, accepts only same-origin paths,
  and does not create an example-specific PageBuilder or pipeline;
- full and isolated builds use the same PageBuilder. The physical index is
  detected before the old projector, so the old descriptor scan/public
  projection is not run for the complete build.

## Checks

```text
full build: 103 pages
source kinds: authored_markdown=103, generated_projection=0
output files: 309
normalized tree sha256:
6583d95fb039ba70f2ebb8d560dcc99d0110b191864d7d9131f42c01176c1216
static: 206 HTML, 21,436 local references, broken=0
```

All fourteen isolated rebuilds match the new full HTML exactly. Parity ledger
SHA-256:
`4dc995e386525104e24fd34817febf7a5a3d399c1b7a214f5b6ec7a968be52b9`.

Focused PHPUnit covers the typed definition source, Markdown compiler,
same-origin positive/negative renderer contract, exact documentation product
matrix and legacy allowlist. The real documentation matrix passes after
asserting 14 authored example routes and absence of both example receipts.

Real-browser checks cover index/detail, 1440 light, iframe result, source code,
localized copy interaction, keyboard focus and zero console errors. Screenshots
are under `browser-physical-examples/`. Dark/mobile are repeated in integrated
acceptance after the retirement commits.

## Semantic changes and parity decision

The URL, purpose, result iframe, exact live sources, outline, breadcrumbs,
previous/next and reader controls are preserved. Physical detail pages now
also appear as children of the examples section in the navigation tree; this
is a deliberate convergence on the same PageBuilder navigation model rather
than a hidden projector-only sequence. No public URL or example meaning
changed.

## Rollback

Revert the candidate commit to restore projector ownership and the exact M4.1
HTML baseline. The canonical demonstrator results were not modified.

## Next deletion gate

Generated ownership is zero, but no legacy file is declared retired by this
checkpoint. M4.3 must first record zero-reference scans, remove the 14-route
allowlist and classify projector consumers outside the current RU contour.
