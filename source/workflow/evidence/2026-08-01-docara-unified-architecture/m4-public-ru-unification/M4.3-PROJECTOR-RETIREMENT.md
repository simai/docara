# M4.3 generated public path retirement

Status: `PASS`

Parent revision: `76a7239c8dd84541a8baac2c9414364fd9749fc1`

Candidate revision: the commit containing this evidence.

## Result

- all 103 selected Russian routes have a physical Markdown owner;
- generated public page owners: `0`;
- the generated-route allowlist contains zero routes and names `none` as its
  owner;
- `PortableDeclarativeExampleProjector` and
  `PortableComponentCatalogProjector` are removed because neither has a
  remaining public or package consumer;
- the legacy example descriptors/schema, demonstrator page templates and
  declarative-example view models are removed;
- `.docara/examples-receipt.json` and
  `.docara/component-catalog-pages.json` are no longer produced or consumed;
- the effective machine-readable `_docara/component-catalog.json` remains a
  legitimate derived catalog and is still verified.

The thirteen detail pages show their own physical Markdown owner as source.
The Russian language file now contains reusable interface labels only.

## Positive evidence

Disposable full build:

- selected pages: `103`;
- authored Markdown pages: `103`;
- generated pages: `0`;
- output path:
  `/tmp/docara-m4-runtime/docs/site/build_m4-retired-projectors-2`;
- normalized full-tree SHA-256:
  `d6928d644fa4180b633e46474fbf61d48a7ab7e7fd953ac87e51ff415525b04f`;
- static verification: `206` HTML, `21,436` local references, `0` broken.

All fourteen `/ru/examples/` routes match their isolated build output exactly.
Parity ledger: `/tmp/m4-retired-projectors-single.tsv`; SHA-256:
`ad6c4388e2bedbb3d78949860d7a837328cf58cb74004ed7d96558f4a0a6582c`.

Focused PHPUnit selection:

```text
PortableSiteBuilderTest|StaticBuildVerifierTest|PortableDocumentationSiteTest|LegacyPublicSourceAllowlistTest
58 tests, 1,144 assertions — PASS
```

## Negative and zero-reference evidence

Repository scans have zero positive runtime references to:

- `PortableDeclarativeExampleProjector`;
- `PortableComponentCatalogProjector`;
- the four removed `DeclarativeExample*ViewModel` classes;
- removed demonstrator detail/index template ids;
- `resources/schemas/declarative-example.schema.json`;
- either removed public page receipt.

The remaining receipt-name assertions are negative tests proving that the
files are absent. The legacy descriptor test proves that adding a JSON file
under the removed descriptor directory cannot publish a route or receipt.

## Deliberately retained package sources

`resources/component-catalog/examples/*`, component definitions/assets and
non-Russian records under `resources/language-packs/` remain package-owned or
future-locale sources. They do not own a current Russian public page and are
not deleted without an independent locale/content parity migration.

## Rollback

Revert the candidate commit to restore the two projectors, descriptors,
templates, receipts and previous allowlist. Revert `76a7239` as well only if
the physical example owners themselves must be rolled back. Generated output
is disposable and is rebuilt; it is never a rollback source.

## Nonclaims

- `DeclarativePipeline::buildGenerated()` and the trusted-main bridge are a
  separate M4.4 deletion gate and remain unaccepted here.
- Full M4, M5, other locales, release and production remain unclaimed.
