# M4.6 reverse-outcome audit

Verdict: PASS_WITH_NONCLAIMS

Audit target: `f2f66d2` (integrated evidence and graph checkpoint).

Runtime target: `45ac1e2`; later M4 commits change only specification, graph,
workflow, handoff and evidence.

## Reverse check from the requested outcome

| Required outcome | Evidence class | Finding |
| --- | --- | --- |
| 103 current public Russian routes have one Markdown owner | proof | 103 selected, 103 physical owners, generated owners 0 |
| 14 `/ru/examples/` routes are no longer generated | proof | physical owners, full/single parity and projector deletion recorded in M4.2-M4.3 |
| one PageBuilder and one typed in-memory IR contour | proof | 103 receipts use `pagebuilder_document_ir`; trusted/generated bridge references 0 |
| one renderer registry and Smart gateway | proof | no second public renderer/registry/gateway or route-specific pipeline found |
| isolated route chosen before unrelated compilation/projection | proof | focused route-selection tests and 103/103 isolated matrix pass |
| derived navigation/search/outline remain functional | proof | full/single equality, search regression test and browser/static matrices pass |
| legacy deleted only after parity and rollback | proof | old-to-new map, zero-reference scans and commits `76a7239`, `7ba1033`, `9bb17e7` |
| deterministic/static result | proof | two 305-file builds byte-identical; 206 HTML, 21,436 references, broken 0 |
| runtime quality | proof | 359 tests/5,935 assertions; lint/format/JSON/YAML/Composer/graph/diff green |
| browser product result | proof | 24 representative cases and 103-route smoke pass; interaction checks pass |
| locale/source boundaries | proof with bounded scope | current RU page prose comes from Markdown; other locales are retained and unclaimed |
| release or production | nonclaim | no merge, push, tag, release or deploy occurred |

## Contradiction scan

- runtime/tests contain zero references to the retired public projectors,
  `trustedMainHtml`, `buildGenerated()` or `buildRendered()`;
- generated-route allowlist is empty;
- the Russian package language pack has zero public messages;
- all 103 Markdown owners are physical files;
- graph validation reports zero warnings and blockers;
- no dependency lock or SIMAI Framework revision changed in M4;
- no user-owned change was overwritten or deleted.

The remaining `InlineComponentRenderer`, declarative parser and package
language repository are not parallel public page owners. They are retained
compatibility/composition or other-locale package APIs and therefore do not
contradict the M4 outcome.

## Notes and nonclaims

1. Global `ACCEPTANCE.md` remains intentionally incomplete. M5 must test the
   exact archive, portable init/update, global author workflow and all remaining
   locale/security/accessibility criteria.
2. `ar`, `en`, `fr-CA` and `zh-Hans` are not declared migrated.
3. M4 does not authorize deleting their package data.
4. Release readiness and production readiness remain `not_claimed`.
5. The installed Docara skill was disabled and explicitly forbidden; execution
   used repository specification, graph, workflow and handoff. The federation
   resolver gap did not alter the repository-owned contract.

## Decision

M4 is complete and independently reviewable. The exact committed candidate may
advance to M5 read-only acceptance. No release action is authorized by this
verdict.
