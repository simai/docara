# M5.1 recovery and execution contract

Status: PASS

Input revision: `900c688fbf320a8e893b4d97838c611526c2a0d8`

Initial state:

```text
pwd: /Users/rim/Documents/GitHub/docara-unified
branch: codex/docara-unified-architecture
HEAD: 900c688fbf320a8e893b4d97838c611526c2a0d8
worktree: clean
```

The independently accepted M4 boundary is the whole-goal rollback. The M5
implementation contract is recorded in
`source/workflow/2026-08-02-docara-m5-product-stabilization-goal.md`.

## Reproduced gap inventory

- `PortableProjectInitializer::initialize()` copies missing stubs and preserves
  every existing file; it has no ownership distinction or transaction.
- `InitCommand --update` calls the same initializer directly; there is no
  verify, diff/dry-run, explicit plan-bound apply or rollback command.
- no machine-readable project ownership manifest is installed;
- package `composer.lock` is intentionally export-ignored, while no shipped
  dependency tuple verifies a consumer-owned lock;
- the existing build promotion is atomic, but the complete failed-partial
  output preservation matrix is not yet explicit;
- global M5 locale/security/accessibility/author acceptance remains open.

M4 product/runtime remains unchanged by this checkpoint. Graph validation,
JSON/YAML parsing and `git diff --check` are required before its commit.
