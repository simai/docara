# M5.7 candidate verification and handoff

Status: PASS for implementation; independent acceptance pending

Implementation revision: `72ad27e34e1dbf2c3a24363d5871bed876e42b68`

Exact archive candidate revision:
`48751b8ca221f7185a72ce19188b1441aea93d2e`

M4 rollback boundary: `900c688fbf320a8e893b4d97838c611526c2a0d8`

## User result

A consumer can install Docara from a Composer ZIP, initialize an empty project
without Node.js, verify package ownership, build the complete starter, rebuild
one selected route through the same PageBuilder and statically verify the
result. The public documentation site remains deterministic across two clean
builds and all 103 current routes reproduce their full-build HTML in isolated
mode.

The first archive integration run found that Composer's generated bin proxy
could initialize a project but could not run from that project's directory.
Commit `b9ae62a` binds the CLI to `_composer_autoload_path`; a second clean dist
install proves the correction. Commits `bb54562` and `72ad27e` exclude graph,
tests, source workflow, local state and repository-only development files from
the consumer archive.

## Exported Composer consumer

Verified pre-handoff archive iteration at runtime revision `b9ae62a`:

```text
ZIP files: 672
SHA-256: 142d386ac37fdbd0aa12995c90ec22340780b3b7b635cd65873ca3a95e5155fe
SHA-1 used by Composer dist verification:
fe550fccad4c1a40d696659ee3a19f9b25a71954
install mode: dist (no package .git directory)
Composer release verifier: PASS
consumer package version: 2.0.0-alpha5
consumer source reference: b9ae62a277872f6accaf085610e922534511b3b2
```

The final independently consumable archive is bound below. The later commit
containing this completed evidence changes only `source/` and `graph/`, both
excluded from the Composer archive.

## Fresh initialized consumer

The clean archive consumer produced:

```text
init: copied=55, package-owned files=27
update --verify --json: status=current; project_owned_preserved=true
full starter build: 38 pages
static verification: 76 HTML, 3,742 local references, broken=[]
/ru/ full HTML SHA-256:
c16dddc9781ca08137a6d06df2c79fed7d02be8aadc100ef5cec346d9ea180bb
/ru/ isolated HTML SHA-256: identical
dependency mode: consumer_composer_lock
moving references allowed: false
```

The receipt records the exact package source revision, package tree hash,
consumer lock hash, direct dependency versions/references, immutable Framework
lock/manifests/assets and production inputs without a private project path.

## Current public site

Two independent clean copies of `docs/site` were built by the archive-installed
candidate:

```text
selected pages: 103 / 103
files per build: 305
recursive byte diff: empty
normalized tree ledger SHA-256:
a7308bf310b3962bc5e50d227e412ee637eb4c4192de291eadf178c47d18380f
static verifier A/B: 206 HTML; 21,440 local references; broken=[]
all-route isolated parity: routes=103; failures=[]
```

All 103 selected routes have physical Markdown owners. Generated public owners,
generated allowlist entries and runtime/test references to the retired public
projectors, `trustedMainHtml`, `buildGenerated()` and `buildRendered()` are
zero.

## Runtime and repository quality

Authoritative disposable runtime: PHP `8.4.20`, PHPUnit `11.5.56`.

```text
PHPUnit: 376 tests, 6,061 assertions, PASS
Pint --test: PASS
PHP lint: 262 tracked PHP files, PASS
JSON: 405 files, PASS
YAML syntax: 33 files, PASS
Composer validate --strict --no-check-publish: PASS
project graph: 1 goal, 6 stages, 9 batches, 6 mappings
graph warnings=0; blockers=0
git diff --check: PASS
```

Focused negative coverage includes update dirtiness/unknown ownership,
symlink/hardlink/traversal/output-root escape, stale plan, damaged rollback,
raw HTML, embed allowlist/sandbox, missing locale copy, unknown component/node
and safe actionable diagnostics. Update apply/rollback and whole-output
preservation after a failed selected-page build are covered by executable
tests and the earlier checkpoint evidence.

## Browser and accessibility binding

The complete M5.5 real Chromium matrix covers 1920/1440/390, light/dark,
Russian LTR and Arabic RTL, reduced motion, search/settings/mobile dialogs,
keyboard focus, Esc trap/return, copy, tabs and tree with zero overflow or
console/page errors. No UI/runtime asset changed after M5.5; later changes are
documentation, diagnostics, CLI bootstrap and archive hygiene.

An exact-candidate Chromium smoke additionally checked `/ru/` at 1440 and
`/ru/components/alert/` at 390: HTTP content rendered one H1, `lang=ru`,
`dir=ltr`, page overflow `0`, Alert present, console errors/warnings `0`.

## Rollback

- whole M5: reset by normal revert sequence to exact M4 boundary `900c688`;
- each implementation checkpoint has its own commit;
- consumer update rollback is a hash-verified package, manifest and lock
  snapshot and does not use generated build output;
- no merge, push, tag, release or deployment was performed.

## Reverse-outcome implementation audit

The implementation evidence proves the requested product behavior, archive
installability, update safety, source boundaries, one component/PageBuilder
contour, locale/security/accessibility fixtures and author workflow. It does
not prove an independent tester has accepted the exact final archive; that is
the sole next architecture gate.

## Final archive binding

```text
candidate revision:
48751b8ca221f7185a72ce19188b1441aea93d2e
archive: docara-2.0.0-m5-candidate.zip
files: 664
SHA-256:
d12169b3c5080f219dada00cc976a758263cbc38ef845da11176ed7e34e8334a
Composer dist SHA-1:
3db0f84f8654221d0a40a544e4b7a1cb66a7986c
install: PASS, dist, no package .git directory
Composer source reference: exact candidate revision
Composer release verifier: PASS
```

The final clean consumer repeated `init`, `update --verify`, full build,
static verification and isolated `/ru/` build. Results remained 38 pages,
76 HTML, 3,742 references, `broken=[]` and exact full/single HTML SHA-256
`c16dddc9781ca08137a6d06df2c79fed7d02be8aadc100ef5cec346d9ea180bb`.
Its receipt records engine tree SHA-256
`ba6a18f5aae4c34b0408f798977b76e546e7bdbd0c8c6688768606183e1f8ddc`,
consumer lock SHA-256
`7e1c0ad824044cfdc02be3b1fbb19eba37c2ecd03b6e12923ac6c23d15390c28`
and immutable runtime tuple SHA-256
`7c89c3177987f0bdddb7db4e13ba4deaef05cdf6f1750203f8460152ad9694ee`.

## Nonclaims

- architecture acceptance is not PASS in this executor thread;
- the EN/AR fixtures are not full documentation translations;
- no default-branch merge, push, tag, release or production deployment;
- ambient deprecation notices come from the locally installed Composer 2.7
  PHAR under PHP 8.4; Composer validation and installation exit successfully.
