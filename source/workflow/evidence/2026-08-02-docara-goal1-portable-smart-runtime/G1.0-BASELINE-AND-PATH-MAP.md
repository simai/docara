# G1.0 baseline and Smart path map

Status: `pass_candidate`
Input revision: `313afa17e21df2299a6276d246cb4508c7ec00b5`
Worktree at intake: only the user-owned accepted architecture plan was
untracked; it was preserved and promoted into this checkpoint.

## Gate and routing result

- federation route: central fallback because no repository-local federation
  route was available;
- stale installed `docara` skill: disabled and not used;
- env policy: PASS;
- repository hygiene: PASS;
- source policy: PASS;
- resolver's historical `2026-07-18-declarative-docara-portable-format`
  workflow belongs to another repository/worktree and was rejected as a false
  association;
- Goal 1 workflow and launch record bind writes only to this worktree and
  disposable `/private/tmp` paths.

## Current Smart ownership/path map

| Responsibility | Current path | Baseline deviation |
| --- | --- | --- |
| one facade | `Declarative/Smart/SmartComponentGateway.php` | namespace `ui.*`/`docara.*` branches select resolver |
| Framework plan | `Declarative/Smart/SmartPlanResolver.php` | separate structural resolver behind facade |
| Docara plan | `Declarative/Smart/CompositeSmartPlanResolver.php` | product manifest assertions and four-ID semantic switch |
| final rendering | `Declarative/Rendering/SmartRenderer.php` | six component IDs select ViewModel methods |
| view preparation | `Declarative/Rendering/ViewModelFactory.php` | six component-specific methods |
| registry | `Smart/SmartRegistry.php` | manual contribution assembly |
| Docara ownership | `Smart/DocaraSmartContribution.php` | four components, views, templates, aliases and assets enumerated in PHP |
| Framework ownership | `Smart/FrameworkSmartContribution.php` | Alert/Button enumerated in PHP |
| templates | `resources/smart/<id>/templates/` | trusted package templates, but registry paths are manually listed |
| public entry | typed Document IR and `DocumentRendererRegistry` | accepted single pipeline; must remain unchanged |

Physical built-in Smart roots at baseline: `docara.brand`,
`docara.navigation`, `docara.preferences`, `docara.toc`, `ui.alert`,
`ui.button`. `resources/smart/assets` is the shared package asset directory.

## False acceptance correction

`ACCEPTANCE.md` previously marked “new component requires no central renderer
branch” as complete. The parser is generic, but `SmartRenderer`,
`ViewModelFactory`, semantic props and contribution PHP prove the broader claim
false. The row is reopened until G1.6 evidence closes it.

## Disposable public baseline

The source project was copied to
`/private/tmp/docara-g1-baseline.zEfJOW/site`; no tracked build output or live
site was touched.

Commands:

```text
cd <disposable>/site
php <repo>/docara build production
php <repo>/docara verify-static build_production
php <repo>/docara build production --page=/ru/components/alert/
cmp <saved-full-alert> build_production/ru/components/alert/index.html
```

Results:

- full build: 103 selected routes, 305 files, 206 HTML;
- static verifier: 21,437 local references, broken=0;
- single Alert build: 1 route, byte-identical Alert HTML to full build;
- Alert HTML SHA-256: `a1ebce7ac2d236ab4890780af60f9781d33ee6da7c2fc43db109feaa0590562a`;
- Button HTML SHA-256: `7ccd35cdcacf5f54c2953ecaf2d7c90947dd0bc0ba2c4625b500f1a5a407f309`;
- root index HTML SHA-256: `38365edd483ebe5c6121d5f4b17cc275fb31f2ab4e55817990ced80845804c5c`;
- sorted file-hash ledger SHA-256:
  `e658d6c6077864f15f945ef8e96f899b2587fb02de54e2455d8a0523f79583f9`.

The first attempted command used a nonexistent `--site` option and changed no
repository/runtime state; the corrected command ran from the copied site root.

## Source-pinned SF5 candidate

Read-only inventory found the tracked Smart v1 contract in committed
`bx-simai.main` revision
`d6f90bba6a9a2f30ac41075d62cf51f1014b7e78`. The external checkout contains
unrelated dirty work, so G1.1 must read committed blobs with `git show`, vendor
the minimal versioned contract snapshot into Docara, and never treat worktree
changes as canonical. The committed artifact includes `schemaVersion=1.0`,
`kind=smart`, manifest/view/preset/template roots and `Smart::render()` /
`Smart::tree()` behavior.

## Rollback

Revert the G1.0 commit only. No runtime file has changed in this checkpoint.

## Checkpoint verification

- focused Smart/Framework baseline: 70 tests, 273 assertions, PASS;
- Pint on current Smart/Declarative Smart surfaces: PASS;
- repository JSON: 519 files, PASS;
- repository YAML: 172 files, PASS;
- project graph: 1 goal, 9 stages, 12 batches, 6 mappings, warnings=0,
  blockers=0;
- `git diff --check`: PASS.
