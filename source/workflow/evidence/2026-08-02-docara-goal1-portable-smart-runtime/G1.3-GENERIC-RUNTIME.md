# G1.3 — Generic Smart runtime

Parent SHA: `97bad5a1fbac51cfbd256f7ba9c9d7515d331115`

## Outcome

The existing `SmartRenderer` now consumes one normalized `SmartInvocation`,
one standard `SmartTemplateContext`, a registered context adapter and a
registered rendering strategy. Component IDs are absent from renderer
dispatch. `ViewModelFactory` and the component-specific semantic props switch
were deleted after the focused parity run.

The registered adapters are host integration boundaries selected by the
source-pinned manifest contract (`provenance.input_adapter`), not by a component
ID branch. Unknown adapters and strategies fail closed.

## Verification

```text
php vendor/bin/phpunit --fail-on-warning \
  tests/Unit/SmartGenericRuntimeTest.php \
  tests/Unit/SmartComponentGatewayTest.php \
  tests/Unit/DeclarativeRenderingTest.php \
  tests/Unit/DeclarativeViewCompositionTest.php \
  tests/Unit/PageBuilderTest.php \
  tests/Unit/Sf5SmartArtifactV1ContractTest.php
```

Result: PASS, 34 tests / 412 assertions on PHP 8.4.20.

Disposable full build: `/private/tmp/docara-g1-g13.fB1Slo/site`.

- 103 routes, 305 files, 206 HTML;
- static verifier: 21,437 references, broken=0;
- every public file is byte-identical to the G1.0 baseline;
- only `.docara/resolved-page-plans.json` changed, because its internal
  provenance hashes now bind the portable strategy/adapter and current engine
  tree;
- Alert HTML: `a1ebce7ac2d236ab4890780af60f9781d33ee6da7c2fc43db109feaa0590562a`;
- Button HTML: `7ccd35cdcacf5f54c2953ecaf2d7c90947dd0bc0ba2c4625b500f1a5a407f309`;
- root HTML: `38365edd483ebe5c6121d5f4b17cc275fb31f2ab4e55817990ced80845804c5c`.

`rg` zero-reference scan for `ViewModelFactory`, the old renderer match and
`assertSemanticProps` is empty. Pint, `git diff --check` and PHP focused lint
pass.

## Rollback

Revert this checkpoint commit. G1.2 remains the independently recoverable
provider/compiler boundary; no public owner or live output was changed.
