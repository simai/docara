# G1.6 — integrated Goal 1 candidate

Status: `PASS_CANDIDATE_AUDIT_PENDING`
Implementation source: `34496d49ce366f1108d2aed37c0adda35f6e5f58`
Parent before the final retirement: `5d9fa5abb2f6d8db2a9d012c03adbb299c695aa7`
Branch: `codex/docara-unified-architecture`
Rollback: revert Goal 1 commits in reverse order; no reset is required.

## User-visible outcome

Framework `ui.alert`, `ui.button`, all bundled `docara.*`, and the portable
project fixture use the same typed in-memory node, provider registry, Gateway,
SmartRenderer and PageBuilder. A project adds `smart/project.notice` under its
declared namespace without editing engine `src/`. Existing public Alert and
Button HTML remains byte-identical to the pre-retirement G1 candidate.

## One-path proof

- `PageBuilder` compiles original Markdown directly; there is no Framework
  placeholder extraction or HTML hydration before Document IR.
- `FrameworkComponentRuntime::extract()` and `FrameworkHostRenderer` were
  deleted after exact Alert/Button parity. The remaining runtime responsibility
  is lock/preflight, deterministic assets and diagnostics for calls already
  rendered by the Gateway.
- `SmartComponentNode` stores deterministic document ordinal and source span;
  its renderer builds one `SmartCallNode`, resolves it through provider
  ownership and invokes the one `SmartRenderer`.
- source scan: no `ViewModelFactory`, contribution classes, component-ID match,
  namespace renderer branch, Framework host renderer or PageBuilder extract /
  hydrate call in production runtime.

## Exact source checks

Environment: macOS, PHP 8.4.20.

```text
PHPUnit: 367 tests, 7,158 assertions, PASS
Focused final boundary/catalog/PageBuilder suite: 20 tests, 1,255 assertions, PASS
Pint --test: PASS
Composer validate --strict: PASS (Composer dependency deprecation notices only)
PHP lint: 265 current source/test PHP files, failures=0
JSON parse: PASS
YAML parse: PASS
git diff --check: PASS
```

Security negatives are covered by provider/runtime suites: traversal, symlink,
namespace collision, duplicate ID/alias, unsafe template, unknown prop/view,
strategy/adapter and mutable Framework reference all fail closed. Authored
Markdown/config cannot choose PHP/template filesystem paths.

## Builds, parity and determinism

Disposable root: `/private/tmp/docara-g1-final2.NfzQfp`.

- two independent full builds: 103 routes, 305 files each;
- tree digest A/B:
  `300eb592287db7f25010b583a3cca585e6a0888b0ab12ee42f8969b4e0142eb2`;
- static verifier A/B: 206 HTML, 21,430 local references, broken=0;
- full/single HTML parity: 103/103 routes;
- Framework Alert HTML:
  `2a6c99e9fb3d80125a669f18513f84d199d5dddcf6a9b94c0cb857b157f35671`;
- Framework Button HTML:
  `134b171116c63430deb27aa9b4437baf8fca1396765917ad65f71a640ba1ab71`;
- both Framework pages are byte-identical to the G1 candidate immediately
  before removal of the legacy path.

The fresh initialized project proof remains bound to the same runtime surfaces:
38 routes, 76 HTML, project Smart CSS published, broken=0, full/single root
parity. The final retirement removes only an unused legacy renderer and does
not alter the project provider/Gateway path.

## Cross-host proof

The exact tracked upstream revision is
`d6f90bba6a9a2f30ac41075d62cf51f1014b7e78`. A clean `git archive` of that
revision accepted the same portable `fixture.notice` artifact and rendered it
through upstream `Simai\Main\UI\Smart::render`. Result HTML SHA-256:
`952a470cd1e347006d6051152ad971672da7b6ff88a525bca2d6a0cd012502e1`.
The portable manifest/view/preset/template/assets are compatible; Docara page
layout and SF5 host bootstrapping remain correctly classified as host-owned.
No external Framework checkout was changed.

## Browser evidence

Fresh HTTP preview served the exact public output on `127.0.0.1:18768` only.

- 1440px Alert: `sf-alert=1`, settings dialog focus/Esc return PASS,
  overflow=0, console errors/warnings=0;
- 390px Button: `sf-button=2`, mobile navigation and search dialogs/Esc PASS,
  open dialogs after close=0, overflow=0, console errors/warnings=0;
- screenshots:
  `.playwright-cli/page-2026-08-02T14-50-27-542Z.png` and
  `.playwright-cli/page-2026-08-02T14-50-56-327Z.png`.

The final exact source removes only the now-unreferenced legacy renderer after
these pages had already moved to the Gateway; its public HTML/assets are
byte-identical to the browsered build.

## Nonclaims and boundaries

- Independent Goal 1 acceptance is not claimed by the executor.
- Goal 2 design registry/preview and Goal 3 SDK/MCP are not started.
- No release identity or production dossier was rebuilt for Goal 1.
- `docara.test`, `docara-new.test` and external Framework repositories were not
  read or written by Goal 1 implementation.
- No merge, push, tag, release, publication or deployment occurred.
