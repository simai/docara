diff --git a/local/modules/simai.main/lib/UI/Smart.php b/local/modules/simai.main/lib/UI/Smart.php
--- a/local/modules/simai.main/lib/UI/Smart.php
+++ b/local/modules/simai.main/lib/UI/Smart.php
@@ -270,3 +270,4 @@ final class Smart
             'props' => true,
             'children' => true,
+            'slot' => true,
         ];
@@ -288,2 +289,2 @@ final class Smart
-        foreach (['view', 'preset', 'children'] as $key) {
+        foreach (['view', 'preset', 'children', 'slot'] as $key) {
             if (array_key_exists($key, $options)) {
@@ -350,2 +351,1 @@ final class Smart
-        $view = (string)($node['view'] ?? '');
         $slot = (string)($node['slot'] ?? '');
