# R1.1 independent M5 exact-archive acceptance

Verdict: `PASS_WITH_NOTES`

Audit handoff id: `DOCARA-AUTO-AUDIT:019fc09b-ce28-7820-b273-1d8b75168aca`

## Exact binding

- implementation handoff HEAD:
  `afc5e0477e0ba9f65765e1cb1016bd996cb8fa75`;
- exact tested source/archive revision:
  `48751b8ca221f7185a72ce19188b1441aea93d2e`;
- archive SHA-256:
  `d12169b3c5080f219dada00cc976a758263cbc38ef845da11176ed7e34e8334a`;
- consumer lock SHA-256:
  `7e1c0ad824044cfdc02be3b1fbb19eba37c2ecd03b6e12923ac6c23d15390c28`.

The independent audit inspected the repository and exact candidate rather than
accepting the executor summary. The archive was not rebuilt or substituted.

## Independently reproduced result

- clean worktree and `git diff --check`: PASS;
- PHPUnit: 376/376 tests and **6,062** assertions;
- Pint, Composer strict, PHP lint, JSON and hygiene: PASS;
- fresh Composer dist consumer without package `.git`: PASS;
- init/verify/full/single/static and no-op update apply/rollback: PASS;
- project-owned content/assets preserved exactly;
- public site: 103 routes, 305 files, two byte-identical builds;
- full/single parity: 103/103;
- static verification: 206 HTML, 21,440 local references, broken=0;
- 103 physical Markdown owners, one `content/ru/lang.json`, zero generated
  public owners;
- ownership/update/rollback, in-memory typed IR, one Smart gateway and one
  PageBuilder: confirmed.

## Evidence discrepancy resolved

M5.7 recorded 6,061 assertions from the executor's PHP 8.4.20 disposable
runtime. The independent exact-source clone produced 6,062 assertions. The
tester count is the accepted independent observation. This is an
environment/reporting difference, not a failed assertion or product defect;
the immutable archive is intentionally unchanged.

## Browser evidence relationship

The complete 1920/1440/390, light/dark, LTR/RTL matrix was captured at the M5.5
UI-equivalent checkpoint. Later candidate commits changed documentation,
diagnostics, CLI bootstrap and archive hygiene, not public runtime/UI assets.
The exact candidate separately passed representative desktop/mobile Chromium
smoke, tabs and copy feedback, with console/page errors=0 and overflow=0.
Therefore the broad matrix is accepted as inherited UI-equivalent evidence and
the representative smoke is the direct exact-candidate binding; it is not
misreported as a complete recapture at the final SHA.

## Acceptance boundary

`docara.batch.m5.accept` and the architecture/product-candidate gate are
accepted. This verdict does not authorize or claim merge, push, tag, release,
package publication, production deployment or complete non-Russian content.

Rollback is the immutable candidate archive plus normal Git recovery to
`afc5e0477e0ba9f65765e1cb1016bd996cb8fa75`; no candidate mutation occurred.
