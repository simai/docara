# R1.3 consumers, builds and compatibility

Status: PASS_WITH_COMPATIBILITY_GAPS

## Fresh dist consumers

Two disposable consumers installed the exact ZIP as Composer `dist`; neither
installed package contains `.git`. Both resolved identical locks:

`176748244149dcf5ddc41dfe6365c17e6547239cce18a5f5e5de6809bdbcacbb`.

Both passed `init`, `verify`, full build and static verification:

- starter routes: 38;
- HTML files: 76;
- checked local links/assets: 3,742;
- broken: 0;
- engine-owned files: 28;
- engine state SHA-256:
  `95a6c80fd438bd90d9144c7602af8e58049b90fb0a2080a7af8d145c2fd0f86c`.

Their 166-file build trees are byte-identical. The `/ru/` HTML SHA-256 is
`c16dddc9781ca08137a6d06df2c79fed7d02be8aadc100ef5cec346d9ea180bb`.

## Current public site

The site shipped in the exact package was built independently in both
consumers:

- 103 routes and 305 output files;
- trees byte-identical;
- static verification: 206 HTML, 21,442 local references, broken 0;
- isolated build parity: 103/103 routes, failures 0.

Full and isolated builds use the packaged CLI and the same PageBuilder.

## Source and quality checks

The complete 378-test suite was run in bounded partitions because a monolithic
local command exceeded the execution-surface time cap. All partitions passed:
15/527, 42/906, 142/3,450, 151/961 and 28/232, totaling 378 tests and 6,076
assertions. Focused update is 7/51 and package verification is 2/7. Pint,
Composer strict validation, PHP lint, JSON, YAML, graph validation and
`git diff --check` pass.

## Compatibility cells

| Environment | Result |
| --- | --- |
| macOS, PHP 8.4.20, fresh exact dist consumer | PASS |
| macOS, PHP 8.2.29, fresh lock resolved on PHP 8.2 | PASS |
| PHP 8.3 | NOT EXECUTED LOCALLY; CI contract exists |
| Linux | NOT EXECUTED AS A COMPLETE MATRIX; CI contract exists |

An 8.4-resolved consumer lock correctly refused PHP 8.2 because its selected
Symfony packages require PHP 8.4. A fresh PHP 8.2 resolution selected compatible
Symfony 7.4 packages and passed install/init/verify/build/static. This confirms
consumer-lock ownership and avoids presenting one platform-specific lock as a
universal dependency graph.
