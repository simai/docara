# R1.5 security, browser and adoption

Status: PASS_WITH_EVIDENCE_NOTE

## Security and source boundaries

- Composer audit on the exact consumer lock: advisories 0, abandoned 0;
- archive traversal, collision, symlink, permissions and forbidden-surface
  policy: PASS;
- repository traversal/include/output escape, raw HTML/embed policy and update
  negatives: PASS in the 378-test suite;
- release archive contains no VCS metadata, tests, workflow evidence, caches,
  credentials or private absolute paths;
- public prose remains in Markdown owners; shared locale UI strings remain in
  `content/<locale>/lang.json`; no mandatory page IR JSON/JSONL exists.

The installed security-review skill has no PHP-specific reference set, so the
repository's own PHP security contract, negative tests and artifact scans are
the applicable evidence. No Framework producer was changed.

## Browser and locale contract

Direct checks against the exact packaged runtime (public UI content identical
to source revision `8c0d145`) passed:

- RU LTR `/ru/`, 1920 px: correct locale/direction, overflow 0, settings
  dialog, dark selection, Esc close and focus return;
- RU mobile `/ru/components/alert/`, 390 px: overflow 0, Markdown tab and copy
  feedback (`Скопировано`, `check`), page errors 0;
- disposable AR RTL fixture, 1440 px: `lang=ar`, `dir=rtl`, Arabic H1,
  overflow 0, console errors/warnings 0;
- multilingual fixture build: 40 pages; static verification 78 HTML, 3,790
  references, broken 0.

The RU landing console contained two informational syntax-highlighter fallback
logs for `shell`; they were not warnings or errors and do not change output.

Exact screenshot capture timed out waiting for external fonts. Therefore R1
does not relabel earlier images as exact captures. The broader light/dark,
1920/1440/390, keyboard/focus/dialog/copy/tabs/tree/search/settings/mobile and
reduced-motion screenshots remain UI-equivalent M5 evidence because no public
UI asset changed after that checkpoint; R1 adds direct exact-runtime assertions
above. This is an evidence limitation, not a hidden green screenshot claim.

## Fresh author/adoption

The two exact dist consumers prove empty init and build. The existing accepted
M5 author matrix covers one-Markdown single-page rebuild, add/rename/delete,
shared `lang.json`, LTR/RTL fixtures and atomic failed partial builds through
the same engine. R1 changes only release packaging and release schemas, so this
behavior is unchanged and is rechecked by the exact consumer/public build and
103/103 isolated parity matrices.
