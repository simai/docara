# R2.2 exact package and production-like consumers

Status: `pass`

The input is the independently accepted unpublished package, not the current
developer tree:

- source revision: `56a2abf8bad05923f689141afc0bb045aa4d6734`;
- ZIP SHA-256: `04c18c95f2599905b1908fae3e326a9cf1ba47f29327ddd88465c4b4b792f753`;
- release-manifest SHA-256:
  `d709d27cc226a3833c05ca62271525dfe48042d967940eaa9e8b9ac6a7185669`;
- checksum-file SHA-256:
  `4cea80db782ae402bdd3c36b9f799afe548dd2fd46fe35c594900141058fdb9b`;
- archive entries: `650`; package `.git`: absent.

An independent `--no-local` clone rebuilt the same ZIP and manifest. The
repository artifact verifier passed against the rebuilt manifest.

## Fresh dist consumers

The unpublished ZIP was installed through an explicit Composer package
repository with version `2.0.0-rc.2`, source/dist reference `56a2abf…` and the
package's real require/autoload/bin metadata. This is test metadata only; it is
not a published package coordinate or tag.

| Environment | Install | Verify/init/build/static | Result |
| --- | --- | --- | --- |
| macOS, PHP 8.4.20, Composer 2.7.1 | fresh dist | all | pass |
| macOS, PHP 8.3.31, Composer 2.7.1 | fresh dist | all | pass |
| Debian bookworm container, PHP 8.3.33 | fresh dist | all | pass |

Each initialized consumer produced `38` selected routes, `166` files and `76`
HTML documents. Static verification checked `3,742` local references with
`broken=0`. Full and single `/ru/` HTML were equal. Ownership verification
reported `current`, `27` engine-owned files and
`project_owned_preserved=true`.

The package documentation project produced `103` selected routes and `305`
files. Static verification checked `206` HTML documents and `21,437` local
references with `broken=0`. Candidate tree SHA-256 is
`457790d4cf212174b7ef34893f8ee3cfc11f8973022c8f28c18348e46f2a3bae`.

Two independent PHP 8.4 consumers using the same immutable consumer lock
produced that tree byte-for-byte. A separately resolved PHP 8.3 consumer also
passed, but its receipt intentionally differs by `composer_lock_sha256`; all
other `304/305` files are equal. A mutable second dependency resolution is
therefore not mislabeled as a reproducible input.

## HTTP preview

The candidate was served on a disposable loopback HTTP origin outside the
active `docara.test` root. All `103/103` canonical `/ru/...` routes returned
HTTP 200. Default-locale aliases, CSS, JavaScript and `search-index.json` also
returned HTTP 200.

No build tree, vendor directory, local Composer repository path or raw HTTP log
is committed as evidence.
