# R2.3 compatibility and security closure

Status: `pass`

## Compatibility

| Cell | Evidence | Result |
| --- | --- | --- |
| macOS / PHP 8.4.20 | exact dist install, init, update verify, build, static | pass |
| macOS / PHP 8.3.31 | exact dist install, init, update verify, build, static | pass |
| Linux Debian bookworm / PHP 8.3.33 | exact dist install in disposable container, init, update verify, build, static | pass |
| Composer 2.7.1 | source/dist reference and consumer lock inspected | pass |

The Linux image digest used for the PHP runtime was
`sha256:107f022053b2222ffd64f957c2a3b4c724c506301c84638e537f9027ed6468f5`.
`unzip` and `git` were installed only inside the disposable container so
Composer could exercise a real fresh dist install.

PHP 8.3 and PHP 8.4 may resolve different compatible transitive versions when
the consumer has no lock. This is expected consumer-owned dependency behavior.
Reproducibility claims require the same immutable consumer lock; compatibility
claims use the platform-appropriate resolved lock.

## Package and dependency checks

- repository artifact verifier: pass, `650` entries;
- Composer audit: advisories `0`, abandoned packages `0`;
- release manifest, per-file hashes and embedded CycloneDX SBOM: verified;
- archive traversal/absolute paths: `0`;
- archive symlink/duplicate/case-collision/mode/timestamp policy: pass;
- private absolute paths: `0`;
- sampled secret signatures: `0`;
- package license: MIT; packaged license/notices surface verified by the
  release-package contract.

The scan was run against the ZIP, not a developer checkout. The exact ZIP was
not modified.
