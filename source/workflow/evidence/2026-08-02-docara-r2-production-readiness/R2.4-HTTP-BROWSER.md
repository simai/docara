# R2.4 exact HTTP and browser/product acceptance

Status: `pass`

The browser read HTML that is byte-equal for all `206/206` HTML files to the
final exact-consumer candidate tree. The disposable copy changes only
mtime-derived machine metadata, not HTML or UI assets. Evidence is bound to
ZIP `04c18c95…` and candidate tree `457790d4…`.

## Matrix

| Route | Viewport | Theme | Direction | Overflow | Console errors | Result |
| --- | --- | --- | --- | ---: | ---: | --- |
| `/ru/` | 1920×1080 | light | LTR | 0 | 0 | pass |
| `/ru/components/alert/` | 1440×1000 | light | LTR | 0 | 0 | pass |
| `/ru/components/alert/` | 1440×1000 | dark | LTR | 0 | 0 | pass |
| `/ru/components/alert/` | 390×844 | dark | LTR | 0 | 0 | pass |
| `/ar/` consumer fixture | 1440×900 | light | RTL | 0 | 0 | pass |
| `/ar/` consumer fixture | 390×844 | dark | RTL | 0 | 0 | pass |

All `103/103` canonical routes passed HTTP smoke. CSS, JavaScript and search
index returned HTTP 200; static verifier remained `broken=0`.

## Interaction checks

- search opens as a dialog, accepts a localized query and Escape restores the
  trigger focus;
- settings switch light/dark, close without losing the route and restore focus;
- mobile navigation opens, Escape closes it and returns focus to the menu
  trigger;
- Markdown tab selection exposes the code panel;
- copy button reports localized `Скопировано` with a `check` state;
- tree branches respond to mouse and keyboard Enter;
- responsive tables do not create page overflow at 390 px;
- reduced-motion emulation reports the reduce media query and no console/page
  error;
- RU uses `dir=ltr`; the physical Arabic consumer fixture uses `dir=rtl`, the
  same engine and the same authoring contract.

The Arabic fixture proves the engine contract, not a complete Arabic
documentation translation.

Machine-readable results and screenshot hashes are in
[`browser-results.json`](browser-results.json).
