# R2.5 `docara.test` delta and deployment dossier

Status: `pass_disposable`; live action remains forbidden

## Read-only live baseline

- Caddy HTTPS root:
  `/Users/rim/Sites/docara.test/build_production`;
- TLS/host check: HTTP 200, certificate verification result `0`, loopback host;
- current files: `322`; current HTML documents: `206`;
- current tree SHA-256:
  `b98ea2f66b733c5146360af68c1fe15b55aa099b33957fe52813772d93ce836f`;
- current directory mtime: `2026-07-30T14:40:21+0300`;
- `.docara-backups`: approximately `2.0G`;
- `.docara-staging`: approximately `123M`;
- site parent and active root are on device `16777233`.

No live file, configuration or service state was changed.

## Exact candidate

- files: `305`; HTML documents: `206`; canonical routes: `103`;
- tree SHA-256:
  `457790d4cf212174b7ef34893f8ee3cfc11f8973022c8f28c18348e46f2a3bae`;
- static references: `21,437`; broken: `0`.

The earlier auditor note described a coarse `121`-path delta. Fresh
file-by-file SHA-256 inventory of the exact live snapshot and exact candidate
supersedes that estimate: `133` paths differ (`112` changed, `19` removed, `2`
added). The complete classification is:

| Group | Changed | Removed | Added | Meaning |
| --- | ---: | ---: | ---: | --- |
| canonical RU HTML | 103 | 0 | 0 | accepted content-first/runtime output |
| build receipts | 1 | 3 | 2 | old generated page receipts replaced by backlinks/component index |
| runtime projections/assets | 8 | 15 | 0 | accepted registry/search/shell changes and removal of unused catalog assets |
| stray file | 0 | 1 | 0 | old `ru/.DS_Store` |

Route sets are equal. No HTML route or required asset is removed. All 19
removals are generated receipts, unused duplicated catalog assets or the stray
`.DS_Store`. The two additions are `.docara/backlinks.json` and
`.docara/component-index.json`.

## Verified cutover sequence

The repository-owned helper is
`scripts/atomic-static-cutover.php`. It accepts only safe sibling basenames,
requires exact current/candidate digests, rejects symlinks, requires one
filesystem, refuses an existing backup target and automatically recovers the
first rename if the second rename fails.

The following shape was executed only in a disposable same-filesystem mirror:

```text
php scripts/atomic-static-cutover.php preflight \
  --parent=<approved-site-parent> \
  --active=build_production \
  --candidate=<verified-candidate-basename> \
  --backup=<unique-precutover-basename> \
  --expected-active-sha256=b98ea2f66b733c5146360af68c1fe15b55aa099b33957fe52813772d93ce836f \
  --expected-candidate-sha256=457790d4cf212174b7ef34893f8ee3cfc11f8973022c8f28c18348e46f2a3bae
```

After explicit user approval and a repeated read-only preflight, replace
`preflight` with `cutover`, run the required HTTP/browser smoke, and use
`rollback` with the same arguments on any stop threshold. The helper preserves
the candidate under its original basename during rollback.

Disposable proof:

1. current digest matched `b98ea2f…`;
2. candidate digest matched `457790d…`;
3. cutover passed;
4. candidate HTTP smoke passed `103/103`;
5. rollback passed;
6. active digest returned exactly to `b98ea2f…`;
7. candidate remained available; backup pathname was absent.

Caddy root is unchanged by the directory-name exchange, so no reload is
required. Each rename is atomic on the filesystem; the two-rename sequence is
fail-closed and has automatic recovery, but operators still treat any observed
4xx/5xx during the bounded exchange as an immediate rollback condition.

## Live window, smoke and stop thresholds

Proposed local window: five quiet minutes with no author/build job running.
Before cutover: repeat current digest, candidate digest, same-device, TLS and
103-route static checks. After cutover: `/`, `/ru/`, representative component,
example, authoring and build routes, CSS/JS/search index, then automated
103-route HTTP smoke and a browser console/overflow smoke.

Immediate rollback on any required-route 4xx/5xx, broken asset/reference,
console/page error, overflow regression, digest mismatch, rename failure or
rollback verification failure.

## Retention proposal

Do not create another copied 100+ MB backup. The rename itself makes the
immediate pre-cutover directory the rollback unit without duplicating bytes.
Keep it through a seven-day acceptance interval. A later separately approved
cleanup may retain the newest three successful release backups plus one known
good baseline and remove older duplicates beyond 30 days. Existing backup and
staging content is untouched until that inventory and deletion are explicitly
approved.
