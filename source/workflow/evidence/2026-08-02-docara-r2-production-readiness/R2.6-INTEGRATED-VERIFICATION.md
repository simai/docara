# R2.6 integrated verification and handoff

Status: `pass`

Integrated governance revision: `209b7ec`

Operational proof revision: `8a4790d`

Accepted product artifact source:
`56a2abf8bad05923f689141afc0bb045aa4d6734`

Exact ZIP SHA-256:
`04c18c95f2599905b1908fae3e326a9cf1ba47f29327ddd88465c4b4b792f753`

## Repository verification

Authoritative PHP runtime: `8.4.20`.

```text
PHPUnit: 392 tests, 6,040 assertions, PASS
Pint --test: PASS
Composer validate --strict --no-check-publish: PASS
PHP lint: 237 files, failures=0
JSON: 436 files, failures=0
YAML: 153 files, failures=0
project graph: 1 goal, 8 stages, 11 batches, 4 metrics, 6 mappings
graph warnings=0; blockers=0
git diff --check: PASS
```

The initial YAML command used `safe_load_file`, which is unavailable in the
installed Ruby/Psych version. The supported `Psych.safe_load(File.read(...))`
path then parsed all 153 YAML files. This was verifier compatibility, not a
repository defect.

## Product and deployment verification

- exact package verifier: 650 files, pass;
- Composer audit: advisories `0`, abandoned `0`;
- exact dist consumers: macOS/PHP 8.4, macOS/PHP 8.3 and Linux/PHP 8.3, pass;
- public full builds with identical package and consumer lock: byte-identical;
- candidate: 103 routes, 305 files, 206 HTML, 21,437 references, broken `0`;
- candidate HTTP: canonical routes `103/103`, failures `0`;
- browser: 1920/1440/390, light/dark, RU LTR/AR RTL, console errors `0`,
  overflow `0`;
- exact current -> candidate -> HTTP smoke -> rollback mirror: pass;
- restored current tree: exact SHA-256 `b98ea2f…`;
- candidate tree: exact SHA-256 `457790d4…`.

## Outcome integrity

The future tag target remains artifact source `56a2abf8…`; neither `8a4790d`
nor `209b7ec` is an alternative package source. Production and publication
gates remain closed. No merge, push, tag, release, publication, Caddy reload or
live site write occurred.

The repository now leaves one user decision: deploy the exact candidate to
`docara.test` using the prepared dossier, or keep the current served build.
