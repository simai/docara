# G2.2 — built-in definition migration

Date: 2026-08-03
Parent: `02ba3c6`
Status: `PASS`

`DefinitionRepository` now resolves layout/view/section/block through the one
`DesignRegistry`. `private const DEFINITIONS` and every literal built-in JSON
path were removed from the facade. Smart manifest/view resolution remains on
the accepted Goal 1 SmartRegistry and Gateway contour.

Focused tests:

```text
php vendor/bin/phpunit --filter \
  'DesignRegistryTest|DeclarativePageCompilerTest|Sf5SmartArtifactV1ContractTest|SmartRegistryTest'
```

Result before the structural assertion: 28 tests / 157 assertions, PASS.

Two disposable trees compared exact input `40f5c5d...` with the working
candidate. Both built 103 routes / 305 files / 206 HTML; recursive diff was
empty. Both static verifiers checked 21,430 local references and reported
`broken=0`. The environment-bound sorted ledger was identical on both sides:
`8b25dd6236c4a2ea155dce4ea64037560bd8a01fc2a6585384098b90e12bb8d8`.

Zero-reference scan over `DefinitionRepository` and `src/Design` found no
`DEFINITIONS` table or built-in path literal. A permanent test guards this.

Rollback: revert the isolated G2.2 commit. Package resource artifacts were not
deleted or rewritten, so the previous facade can be restored without data
migration.
