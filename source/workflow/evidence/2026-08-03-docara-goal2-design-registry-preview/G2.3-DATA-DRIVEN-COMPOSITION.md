# G2.3 — data-driven composition

Parent: `e032aca`. Candidate: the isolated G2.3 implementation commit that
contains this evidence.

The layout artifact declares configuration defaults, arbitrary named regions
and the document region/section/slot/block contract. Section and block
compatibility is resolved from registered artifacts. Smart blocks admit any
component accepted by the single SmartRegistry/Gateway; composition code has
no component ID list.

The project-only fixture under `tests/fixtures/design/project/` contributes
`acme.docs`, `acme.article`, `acme.document` and bounded View Trees. The
production compiler renders it without fixture-specific engine source.

Full PHPUnit: 391 tests / 7,558 assertions, PASS. Pint passed after formatting;
Composer strict and `git diff --check` passed.

```text
/Applications/ServBay/package/php/8.4/current/bin/php ../../docara build g23-check
/Applications/ServBay/package/php/8.4/current/bin/php ../../scripts/verify-static-build.php build_g23-check
```

Result: 103 routes, 305 files, 206 HTML, 21,430 local references, broken=0.
Compared with accepted `build_production`, all public files are identical;
only `.docara/resolved-page-plans.json` differs because artifact hashes and
provenance intentionally changed.

Permanent structural coverage rejects concrete design/Smart IDs in resolver,
compiler, layout descriptor and block factory. Existing provider tests cover
traversal, symlink, duplicate and namespace collision failures.

Rollback: revert the isolated G2.3 commit. No legacy artifact was deleted and
no external site was modified.
