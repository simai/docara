# G2.4 — production-faithful preview

Parent: `688841b`. Candidate: the isolated G2.4 commit containing this file.

PreviewKernel invokes the same `PortableSiteBuilder` used by `docara build`.
Its full/single cache is named `build_preview-cache` and carries an explicit
`accepted_build_receipt=false` marker. PreviewShell publishes only the selected
artifact and a preview manifest under `.docara-preview/output/<target>`.

Smart, region and layout fragments are DOM-extracted from the exact production
page output. Page preview is byte-identical to that output. The report reuses
the production plan hash and asset list and contains only project-relative
dependency paths. No second parser, renderer, Gateway, composer or PageBuilder
exists.

Focused test result before command/watch separation: 3 tests / 27 assertions,
PASS. It covers all four targets, independent DOM extraction equality,
production provenance, atomic isolated publication and unknown-target failure.

Manual exact CLI proof from `docs/site` produced Smart Alert HTML SHA-256
`847c86293c87a304259c82607e978a6b2967d58a67840d2fac3509294651a80d`
with production plan hash
`0b9d50c7e691c793990a08d27d81d7dc5acea29f2332124c2a1117c730d4a9a2`.

Rollback: revert the isolated G2.4 commit. Preview output is disposable and no
normal `build_*` output or external site was modified.
