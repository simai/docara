# G2.5 — preview CLI and PHP watch

Parent: `2f6bc5c`. Candidate: the isolated G2.5 commit containing this file.

`docara preview` accepts exactly `smart`, `region`, `layout` or `page`, requires
a public route and requires a safe component/region selector where applicable.
`--json` suppresses the banner and emits canonical JSON. Errors use the stable
`docara.cli_error.v1` envelope and a non-zero exit code.

The PHP-only watcher fingerprints the selected page/config trace, locale
`lang.json`, locale assets and confined project `design/`, `smart/` and
`assets/`. A change invokes PreviewKernel for the same route, which uses the
normal single-page PageBuilder path against its isolated preview cache. It does
not compile or publish a second route.

Focused result: 3 tests / 33 assertions, PASS. Covered outcomes include stable
JSON success/error, watch with a bounded no-change cycle, a changed Markdown
dependency causing exactly one target rebuild, and project-relative dependency
paths.

Manual command:

```text
/Applications/ServBay/package/php/8.4/current/bin/php ../../docara preview smart --page=/ru/components/alert/ --selector=ui.alert --json
```

Result: exit 0, canonical artifact report, `accepted_build_receipt=false`.

Rollback: revert the isolated G2.5 commit. The preview cache/output directories
are disposable and ignored; no normal build or external site was modified.
