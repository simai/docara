# G2.6 integrated acceptance evidence

Date: 2026-08-03
Status: `ready_for_independent_audit`
Input revision: `40f5c5d14dce74383a57d408fd593507addd43d0`
Implementation candidate: `33a377758f12d02a34e50c2f4f6d2aa760cf678b`
Accepted Goal 1 runtime: `44acc1ff91233fa78140222fcb0589bf55b65ca0`
Exact SF5 adapter: `b3cdff87563ff78e7eddf044048a4b298fc69036`

## Outcome

Goal 2 adds one deterministic DesignRegistry for package and project layouts,
View Trees, sections and blocks. Composition is descriptor-driven, and the
project fixture adds `acme.*` design artifacts without an engine source edit.
Preview calls the normal PortableSiteBuilder and extracts bounded targets from
that output; it does not own a parser, renderer, Gateway, LayoutComposer or
PageBuilder and cannot emit an accepted production receipt.

## Focused and full verification

PHP runtime: ServBay PHP 8.4.20.

```text
focused Goal 1 + Goal 2 runtime: 18 tests, 255 assertions, PASS
focused runtime + docs + context: 40 tests, 1,907 assertions, PASS
full PHPUnit: 394 tests, 7,612 assertions, PASS
Pint --test: PASS
Composer validate --strict: PASS
PHP lint: 327 files, PASS
JSON validation: PASS
project context: PASS, issues=0
project graph: 1 goal / 10 stages / 13 batches / 6 mappings,
               warnings=0, blockers=0
git diff --check: PASS
```

The focused matrix covers the DesignRegistry/provider, DefinitionRepository,
RegionCompositionResolver, View Tree renderer, production preview, watcher,
project-local design composition, Goal 1 generic runtime and exact SF5
cross-host fixture. The cross-host fixture remains byte-identical at
`7133c5dcd44aa85f351a85c61c280aa883abd5cdb3c91206168ad63ada497b38`
with empty stderr/warnings.

Security negatives cover duplicate ownership, namespace collision, traversal,
symlink escape, invalid artifact/schema/path, unknown View Tree constructs and
incompatible composition. Project config cannot provide executable PHP or a
template path.

## Build, preview and static proof

Two independent disposable full builds from the candidate produced:

```text
routes: 103
files: 305
HTML: 206
recursive diff: empty
full ledger SHA-256: a4045e033cea8ab7a8bb1ebde5d550fba42dc16482d41ad837c6a3fd7130e7eb
Alert HTML SHA-256: a948b13465c5b2050107fd7ec3f44d7985c81219b5116bdcda6a0535f7b12703
static references: 21,421
broken links/assets: 0
```

Rebuilding only `/ru/components/alert/` over a copy of the full build leaves
the complete file ledger unchanged. Running
`docara preview smart --page=/ru/components/alert/ --selector=ui.alert --json`
creates an explicitly non-accepted preview manifest. Its full-page
`index.html` has the same Alert hash shown above; the bounded Smart artifact
hash is `847c86293c87a304259c82607e978a6b2967d58a67840d2fac3509294651a80d`.

## Structural proof

The active composition surfaces were scanned for concrete design and Smart
IDs. `RegionCompositionResolver`, `DeclarativePageCompiler`,
`LayoutDescriptor` and `ResolvedBlockFactory` contain no `docara.docs`, shell
region/section IDs, `ui.alert` or `ui.button` decisions. A permanent test also
rejects restoration of such lists. `DefinitionRepository::DEFINITIONS` has
zero runtime references and the registry facade is the sole definition path.

The accepted pipeline remains:

```text
Markdown -> typed Document IR -> NodeRendererRegistry
         -> SmartComponentGateway -> LayoutComposer -> PageBuilderResult
```

## Browser evidence

The exact final full build was served from a disposable PHP HTTP server.

| Matrix | Result |
| --- | --- |
| 1920 light | route/title/regions/Smart shell correct, overflow=0 |
| 1440 dark | computed color-scheme dark, overflow=0 |
| 390 mobile dark | mobile navigation present, reduced-motion=true, overflow=0 |
| Search/settings | dialogs open, focus enters, Esc closes and returns focus |
| Tabs/copy | Markdown tab selects its matching panel; copy handler executes |
| RTL fixture | `dir=rtl`, 23 logical-property rule groups, overflow=0 |
| Console | errors=0, warnings=0 |

Screenshots are stored under `browser/`. The RTL capture is an engine/layout
fixture, not a claim that the full Arabic documentation corpus exists.

## Governance, rollback and nonclaims

Canonical graph, generated project context and START/STATUS/ACTIVE/NEXT/RESULT
route only to `independent_goal2_reverse_outcome_audit`. The context freshness
gate is state-driven and retains positive/negative stale-handoff regressions.

Rollback is the ordered revert of Goal 2 commits
`33a3777`, `0c2a9e9`, `2f6bc5c`, `688841b`, `e032aca`, `02ba3c6`, `a874072`.
The accepted Goal 1 candidate is the recovery boundary. Preview/build outputs
are disposable and no external site was changed.

This evidence does not accept Goal 2, authorize Goal 3, or approve merge,
push, tag, release or deployment.
