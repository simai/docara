# C2.1-C2.4 correction evidence

Date: 2026-08-03
Input: `4c0f623403773c00669ef53782785d750839de0a`
Rejected candidate: `33a377758f12d02a34e50c2f4f6d2aa760cf678b`
Correction implementation checkpoint: `c10713429bd44ad8febb323a4d89dcd9a4fedb12`
Exact product/docs candidate: `39f1e3f6e97d7f8138e892b5884ba194cc889a7f`

## Receipt boundary

`PortableSiteBuilder` writes `build.purpose=production|preview` inside the
normal resolved-plan receipt. PreviewKernel requests `preview`; it does not
write an adjacent trust marker. Production/static verification of the exact
preview cache exits 1 and reports:

```text
[PREVIEW_BUILD_PURPOSE_FORBIDDEN] Preview-purpose output cannot be accepted as a production static build.
```

The ordinary exact-candidate full build passes the same verifier.

## Isolated preview output

PreviewShell copies only safe regular non-HTML runtime/content assets from the
preview-purpose build and never copies `.docara/`. It atomically publishes the
selected production page as `index.html`, the fragment as `artifact.html` and
the machine result as `preview.json`.

Exact fixture result:

```text
Smart artifact HTML SHA-256: 847c86293c87a304259c82607e978a6b2967d58a67840d2fac3509294651a80d
Preview index SHA-256: 0f1119620446604f6584b3a1c76b6d97086ecac4cfee3482b0dc4fbed7341924
Preview manifest SHA-256: 647e00c49b0a3b373604060c93681a055e2df3031b774ae0b615c54e4166ec3e
Published non-HTML files: 87
Published file ledger SHA-256: 5278beb717c34e29fa373c413f4ac7de4e36e81bb5b8ef83a9b966f720c46718
```

HTTP HEAD returned 200 and correct MIME for the root HTML,
`ru/_docara/declarative-shell.js`, Framework Alert JavaScript and Smart
navigation CSS. Server logs contain 200 for every requested local asset.

## Target dependency closure

The dependency graph uses project-relative files plus explicit package/project
file/tree identities. It derives content/config/lang from the resolved input
chain, Design artifacts from the selected descriptors, Smart artifacts from
rendered/source identities, and production publisher/runtime assets from the
same page context.

Permanent tests prove:

- Alert excludes `@project-tree:smart/project.notice`;
- unrelated project.notice edit causes zero rebuilds;
- selected content edit causes one route rebuild;
- selected package Smart edit causes one rebuild;
- selected project Smart tree create and delete each cause one rebuild;
- symlink/traversal/unknown targets remain fail-closed.

`target_only` is derived from `dependency_scope=selected_target` plus the
single-page result of each rebuild; it is no longer a constant.

## Registry-time View Tree validation

`JsonSchemaValidator` implements exact-one `oneOf` and rejects malformed
schema branches. JSON Schema patterns translate `\uXXXX` to PCRE Unicode code
points and invalid expressions fail as `SCHEMA_INVALID`. The View Tree region
uses a safe identifier pattern, not a five-region enum.

After schema validation, DesignRegistry runs ViewTreeInspector and matches
layout regions and section slots to descriptor-owned contracts before the
registry is returned. Tests reject bogus kind, tag, attribute, utility,
region/slot mismatch and accept project region `stage` without engine IDs.

The full documentation suite exposed and corrected the formerly latent `/`
href pattern ambiguity instead of weakening `oneOf`.

## Rollback

Revert `39f1e3f`, then `c107134`; revert the workflow checkpoint `6df407a` only
if the correction history itself is no longer required. No external or live
state participates in rollback.
