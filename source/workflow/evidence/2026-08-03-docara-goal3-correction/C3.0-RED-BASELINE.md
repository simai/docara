# C3.0 — independently reproduced RED baseline

Input HEAD: `17be51d90c7e68c7389c917b2c4c0d0697dc35c3`
Rejected implementation candidate: `8cd695ffdef2adf3fa4475b4d0d3e9ba948da560`
Status: `RED_CONFIRMED`

All reproductions used a disposable copy of `stubs/portable`, repository CLI
and ServBay PHP 8.4. No live/test site or external owner repository was used.

## Preview root escape

Setup: `.docara-preview` was a symlink to an empty directory outside the
project root. Command:

```text
php docara qa smart ui.alert --page=/ru/components/alert/ --dry-run --json
```

Observed:

- exit `0`, operation status `success`;
- external file count changed from `0` to `90`;
- empty external ledger `e3b0c442...` changed to
  `acd8ccb72d82db1d5cc9e8c010f4037d3763e10911c03bf22e1138e81cda1280`;
- external tree contained preview HTML, Docara/Framework CSS/JS, brand assets,
  search/component metadata and runtime assets.

This proves mutation occurred through the generated-root symlink before any
containment gate.

## QA plan pre-rejection escape

Setup: `.docara-qa` was a symlink to an empty outside directory. The same
command returned exit `2` and `QA_ROOT_UNSAFE`, but the outside directory gained
`plans/` before rejection. File count remained zero, therefore file-only
ledgers would miss the mutation. Directory inventory changed from empty to one
external directory.

## Diagnostic contract drift

Actual command:

```text
php docara inspect smart project.missing --json
```

Observed CLI result:

- exit `2`, code `SMART_REGISTRY_COMPONENT_NOT_FOUND`;
- operation/subject became `inspect` / `inspect`, losing the requested subject;
- `source=null`, diagnostic provenance `{}`;
- result provenance used `engine_revision=unresolved` and an all-zero input
  hash despite an initialized project;
- owner and suggestion existed but source/pointer/provenance did not.

The same fixture through MCP returned `isError=true` but changed the operation
to `mcp.tool`, subject to `null`, and retained null source/empty provenance.
Human output was derived from the same incomplete fallback result.

## Correction requirements derived from RED

1. Generated-root safety must include directories as mutations, not only file
   hashes.
2. Containment must run before the first mkdir/delete/copy/write/rename.
3. CLI and MCP must invoke one exception mapper with the original operation,
   subject and initialized project provenance.
4. Permanent tests must execute real commands/adapters, not only hand-authored
   result fixtures.

Rollback: no repository runtime file changed in this reproduction; disposable
directories are outside the repository and are not product evidence.
