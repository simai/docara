# C3.1 — project-contained generated writes

Date: 2026-08-03
Implementation commit: `496df2cab420a5f93560ea376ae44417368d1ba4`
Parent: `cda689d997dcb90d74f36e9645e5fb88dcd6030a`
Runtime: `/Applications/ServBay/package/php/8.4/8.4.20/bin/php` (`PHP 8.4.20`)

## Outcome

One `ProjectFilesystemGuard` now owns the pre-mutation policy for preview,
QA, scaffold and the MCP-delegated application services. It validates the real
project root, every existing ancestor, symlink and hardlink state, containment,
case collisions and owned generated roots before delete, mkdir, copy, write or
rename. File publication is no-overwrite and atomic; preview directory swaps
retain the previous accepted preview until the candidate move succeeds.

## Red to green exploit proof

The two independently reproduced RED attacks from `C3.0-RED-BASELINE.md` were
replayed through the real `docara qa smart ui.alert --page=/ru/components/alert/
--dry-run --json` command after the correction:

| Attack | Exit/code | External objects after command |
| --- | --- | ---: |
| project `.docara-preview` symlink to external root | `2` / `SDK_WRITE_PATH_UNSAFE` | 0 |
| project `.docara-qa` symlink to external root | `2` / `SDK_WRITE_PATH_UNSAFE` | 0 |

Disposable cases were `/tmp/docara-g3c-green-preview.pHiC3L` and
`/tmp/docara-g3c-green-qa.nxP6LC`. The external ledgers were enumerated after
each command and were empty. These paths are disposable evidence references,
not product inputs.

## Permanent matrix

`tests/Unit/Goal3WriteContainmentTest.php` executes the real Symfony commands
and covers preview-root and QA-root escape, nested preview output/candidate and
build-cache symlinks, QA result-parent symlink, hardlinked plan, case/collision
without overwrite and scaffold/MCP-owned plan-root escape. Existing scaffold
regressions retain traversal, stale-plan, duplicate namespace, symlink,
hardlink and rollback/no-partial-write coverage.

Command:

```text
/Applications/ServBay/package/php/8.4/8.4.20/bin/php vendor/bin/phpunit --filter 'Goal3WriteContainmentTest|QaServiceTest|PreviewKernelTest|ScaffoldServiceTest|DeveloperValidationTest|McpAdapterTest'
```

Result: `25 tests`, `170 assertions`, PASS.

Formatting and whitespace:

```text
/Applications/ServBay/package/php/8.4/8.4.20/bin/php vendor/bin/pint <C3.1 files>
git diff --check
```

Result: PASS after the formatter normalized `ScaffoldService.php`.

## Rollback

Revert implementation commit `496df2cab420a5f93560ea376ae44417368d1ba4`.
No project content, external root, live site, lock, release or generated build
was committed or mutated by this checkpoint.

This is a bounded implementation checkpoint. Goal 3 remains open and is not
accepted until C3.2–C3.5 are independently ready.
