# C3.10 — finalized production reference seal

Date: 2026-08-03
Status: PASS at exact product candidate `2a7237bc59265d976b6871cb637e7ae67ca2c00b`
Audit marker: `019fc7e0-82bc-7f71-9394-86fd0d4a2163`

## Corrected trust chain

QA now uses a two-phase, PHP-owned chain:

```text
immutable draft plan
  -> browser-recorded reference draft
  -> PHP finalize/reference seal
  -> immutable finalized plan
  -> browser candidate report
  -> authoritative PHP verify
```

The finalized `docara.qa_reference.v2` identity covers contract/version,
subject and target binding, page/artifact hashes, the ordered scenario list,
every screenshot path and every screenshot SHA-256. Its complete canonical
manifest SHA-256 is anchored in `docara.qa_plan.v2`. Verification recalculates
both values before reading a candidate report.

The permanent actual-command regression
`QaServiceTest::coherent_reference_candidate_and_report_mutation_is_rejected_under_original_plan`
changes reference PNG bytes, reference manifest hashes, candidate PNG bytes,
candidate/report hashes and the claimed zero diff under the original plan. The
real Symfony command exits `2` with `QA_REFERENCE_ID_MISMATCH`. Existing plan,
reference-only and forged-candidate mutations remain rejected.

Focused QA/diagnostic/MCP/application checks pass at 18 tests / 144 assertions;
the wider correction-focused matrix passes at 31 tests / 1,647 assertions.
Rollback is the parent of `2a7237b`; no external repository or site was
written.

