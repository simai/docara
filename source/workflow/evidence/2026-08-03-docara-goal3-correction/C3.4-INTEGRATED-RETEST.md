# C3.4 integrated retest

Date: 2026-08-03
Status: superseded after independent audit; historical PASS claims only
Exact product candidate: `a027c9ab399c62f6ecaa830a73d4f44684e7ed81`

## Regression and production path

- full PHPUnit on ServBay PHP 8.4.20: 429 tests, 7,891 assertions, PASS;
- focused containment/diagnostics/scaffold/QA/MCP/package/docs/context/SF5 set:
  46 tests, 1,782 assertions, PASS;
- exact SF5 cross-host regression: 1 test, 44 assertions, byte-identical HTML;
- two repository public builds: 104 routes, 307 files, 208 HTML, identical
  ledger SHA-256 `a66beef90f5d1457525a9a78cff6b9ad350802aa3c6ef1e9548577a7063c3d02`;
- representative `/ru/components/alert/` single build preserved that full ledger;
- static verification: 21,842 local references, broken=0.

The candidate adds one source-gate correction after the visual checkpoint:
`DocumentationContractTest` excludes disposable `.docara-preview` and
`.docara-qa` outputs from the tracked/source documentation vocabulary scan.
The correction changes no runtime, public content, schema or release surface.

## Deterministic package

Two independent `git clone --no-local` checkouts at the exact candidate ran:

```text
php scripts/build-release-package.php \
  --revision=a027c9ab399c62f6ecaa830a73d4f44684e7ed81 \
  --version=2.0.0-alpha4 \
  --tag=goal3-correction-audit-a027c9a --output=<isolated-output>
```

The parameters are evidence labels only; nothing was tagged or published.

- ZIP SHA-256: `32a638b14ac14cc653ba1c5f8745eb9780d5b177a9258d1a3b38f9fe67cd8339`;
- external manifest SHA-256: `9a24b0400824b73e79c9b55b0c9b07db5a84a4275b9ee9fc95e1f73a49cfb11b`;
- checksum file SHA-256: `de4abe64d3c1f75a61a5ce1e0677faacdb7a30ac281d89314c784598ce76369a`;
- file count: 733;
- both ZIPs and manifests were byte-identical;
- `scripts/verify-release-package.php` accepted both exact artifacts.

## Two fresh Composer consumers

Both disposable consumers used the same `composer.json`, exact package
repository metadata, ZIP and immutable `composer.lock`:

- lock SHA-256: `2d56b4d55b0a664ae8bec6cb0d7ab927538ac346b824d7ab8c0d5c86fdbfb454`;
- package `.git`: absent; `node_modules`: absent;
- clean init, doctor, full build, static verification and selected Alert build:
  PASS in both;
- 38 routes / 168 output files per consumer;
- static: 76 HTML, 3,743 local references, broken=0;
- full/single and cross-consumer output ledger SHA-256:
  `d0ac3a928a75ff2561d6413d94d4ea6507ecd35252c2936256a141b46fb53c0f`;
- doctor bound both consumers to engine revision
  `a027c9ab399c62f6ecaa830a73d4f44684e7ed81`.

Composer under PHP 8.4 emitted only Composer-owned deprecation notices; strict
validation and audit remained green. An earlier disposable attempt used two
different consumer locks and was rejected as evidence before this same-lock
matrix; it is not part of the PASS claim.

## Other gates

Pint, Composer strict/audit, PHP lint (325 files), JSON (483), YAML (48),
project-context, canonical graph validation, secret/private-path scan and
`git diff --check` passed. The graph validator reported 1 goal, 11 stages,
14 batches, 7 mappings, warnings=0 and blockers=0.

Rollback is the parent of the exact product candidate. This checkpoint does
not accept Goal 3 and authorizes no Goal 4, merge, release or deployment.
