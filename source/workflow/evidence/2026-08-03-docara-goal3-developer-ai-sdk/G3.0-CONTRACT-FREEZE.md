# G3.0 contract freeze

Date: 2026-08-03
Status: `PASS`
Parent: `adb27f1acde6dfa5f018f7b2e3c2f20b404a0ed2`

## Accepted inputs

- independent Goal 2-C verdict: `PASS_WITH_NOTES`;
- product/docs candidate: `39f1e3f6e97d7f8138e892b5884ba194cc889a7f`;
- exact SF5 adapter: `b3cdff87563ff78e7eddf044048a4b298fc69036`;
- worktree at recovery: clean;
- federation route gap: selected owner `docara` is intentionally disabled;
- reversible local-work action gate: PASS, no blockers.

## Frozen decisions

1. `docara.operation_result.v1` is the only command/service result envelope.
2. Diagnostics use stable structured fields and deterministic ordering.
3. Scaffold apply requires a persisted exact dry-run plan and rechecked hashes.
4. Only project-owned Smart/design roots are writable in Goal 3 scaffolding.
5. `tools/mcp-docara/` is optional PHP stdio tooling and delegates services.
6. Normal consumer build remains PHP-only, Node-free and MCP-free.
7. Golden human/JSON fixtures precede implementation changes.

## Checks

```text
ApplicationContractTest: 2 tests, 6 assertions, PASS
project-context generate/check: PASS, issues=0
JSON and graph structural validation: PASS
git diff --check: PASS
```

The positive doctor and negative stale-scaffold fixtures are canonical JSON,
match `operation-result.schema.json`, and each has a non-empty human twin.

## Rollback

Revert the G3.0 governance/fixtures commit. No product runtime or external state
is changed by this checkpoint.
