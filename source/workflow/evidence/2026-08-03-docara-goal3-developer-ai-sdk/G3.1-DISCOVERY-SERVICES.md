# G3.1 — discovery services

Candidate commits: `7f9f9fb` and `e15a2ae`.

`ProjectRuntime`, `DiscoveryService` and the four commands `doctor`, `list`,
`inspect`, `schema` read the accepted Smart/Design providers and schemas. Human
and `--json` output are projections of `docara.operation_result.v1`; unknown,
ambiguous and invalid provider inputs return stable diagnostics and exit codes.

Focused proof: `DeveloperDiscoveryCommandTest`; the exact dist consumer doctor
reports 7 Smart, 15 design descriptors and 26 schemas with no diagnostics.
Rollback: revert `7f9f9fb`; no public build source is migrated by this batch.
