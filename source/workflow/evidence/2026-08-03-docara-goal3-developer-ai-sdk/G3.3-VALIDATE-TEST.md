# G3.3 — validate and test

Candidate commit: `e15a2ae`.

`ValidationService` delegates manifest, props, view, preset, template, asset,
hydration, fixture/state, accessibility/AI, namespace and provenance checks to
the accepted registries/contracts. `ArtifactTestService` renders through the
same `PreviewKernel`; it owns no parser, renderer, registry, Gateway, composer
or PageBuilder.

Focused proof: `DeveloperValidationTest`, including invalid and unknown
artifacts. Rollback: revert `e15a2ae` without changing Goal 1/2 runtime.
