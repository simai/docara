# C1.0 rejected baseline and RED outcomes

- branch: `codex/docara-unified-architecture`;
- rejected product: `ae6a1e918e248517b728cf40460d6c359991b66e`;
- rejected governance: `26a212aff337254f75cf4df426c0684e5ee8f4e9`;
- worktree before correction: clean;
- independent fresh tree: `c778f629219eb38de4ab49ce111bf6b4dd53771043568572383852d2e3d05376`;
- published stale tree: `b8b47a837f2ac067434a8da27fd950d93e99916f107441dec42dedb3c9843e81`.

## Reproduced semantic failures

1. Atlas has 50 entries, but `kind=smart owner=ui support=supported` and
   `kind=smart authoring=inline` are empty because owner package and namespace
   are conflated and inline/typed definitions are absent from that projection.
2. `project("presentation", "shared")` is empty because the selected surface
   is under `$defs`; most site rows point only to `#`, and composition and
   conditional constraints are not traversed.
3. Public examples use positional Smart selector and nonexistent
   `--apply-plan`; the real contracts are `--selector=<id>` and
   `--apply=<exact-plan-sha256>`.
4. Structural tests asserted source strings or synthetic entries rather than
   the rendered docs/site ProjectRuntime outcomes.

The correction must turn each item into a permanent real-project regression.
