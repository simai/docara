# C1.1 — truthful admitted catalog

Exact product candidate: `eb35f5c6f18e5eb9be69e91887b09486f5703136`.

One `DesignAtlasService` projection now exposes namespace, owner package,
origin, provider, authoring kind and support as independent facets. The exact
docs/site build contains 85 admitted Atlas entries with content SHA-256
`389af5552befda753586c262c908952c41f4f2ef47cd8007be9c592b60c1769f`.

Real-project outcome tests and rendered HTML prove:

- inline Docara: `docara.badge`, `docara.button`, `docara.icon`, `docara.kbd`;
- Framework supported Smart: exact admitted `ui.input`, `ui.dropdown`,
  `ui.checkbox`, `ui.list-item` entries, while owner remains
  `simai/bx-simai.main`;
- block Docara uses the public typed block catalog;
- content containers render only `docara.grid`, not page Layout/Section;
- `docara.grid` publishes `allowed_children=[docara.card]`, `min_children=1`,
  `max_children=12`, `order=declared`, `max_depth=2`.

The six Markdown owners remain physical files under
`docs/site/content/ru/components/`. No prose moved into PHP or JSON. Focused
catalog/settings/journey integration: 40 tests / 2,958 assertions; final full
suite: 484 tests / 10,222 assertions.

Rollback: revert product commits `770cbed` and `eb35f5c` after reverting the
documentation checkpoint `27ffac8` as one bounded correction unit.
