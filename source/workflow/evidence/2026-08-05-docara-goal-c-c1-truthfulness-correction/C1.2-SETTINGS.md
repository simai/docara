# C1.2 — exhaustive settings projection

`PortableSchemaReferenceProjector` resolves selected `$defs` through the
site/section/page entry schemas, follows safe internal refs, and traverses
objects, arrays, `oneOf`, `anyOf`, `allOf`, `if`, `then` and `else` branches.
It emits exact JSON pointers and material constraints without inventing
defaults.

The exact build receipt is
`42f58acb61573a0c278c6fba7ab093998dc0e22be2c6c149c6e836817f5d38e8`.
The `/ru/settings/` shared presentation table contains 77 data rows (78 `tr`
including its header); `/ru/settings/site/` contains 112 data rows. Examples
include exact pointers for nested region sections/items, `uniqueItems`,
`propertyNames`, conditional branches and the valid `minimum=0` constraint.
No output provenance ends at a bare `#`.

Permanent real-project tests verify non-empty root/shared surfaces,
representative nested paths, pointer fidelity and constraint summaries. The
static verifier validates the schema receipt and every build remains derived
from the tracked authoritative schemas.
